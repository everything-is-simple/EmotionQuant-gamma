# Phase 9C / Gene 组合候选卡

- 状态: `Active`
- 日期: `2026-03-19`
- 类型: `组合候选子阶段`
- 范围: `只允许从已经赢下 isolated round 的 Gene 字段里，冻结最小组合候选`

## 1. 这张卡回答什么

这张卡只回答一个问题：

`在多个 Gene 字段已经各自完成 isolated win 之后，哪一个最小组合值得进入正式组合验证，而不是继续停留在口头联想。`

## 2. 为什么现在可以开工

当前真实门槛已经满足：

1. 已有 `3` 个 isolated winner：
   - `duration_percentile >= 95`
   - `reversal_state == CONFIRMED_TURN_DOWN -> exit-preparation only`
   - `context_trend_direction_before -> parent-context negative guard`
2. `GX8 / three-level trend hierarchy` 已完成，不再阻塞组合 scope
3. `wave_role` 已被正式裁定为 `retain_sidecar_only`，因此不会被偷带进组合

所以当前真实状态已经从：

`还不能开卡`

变成：

`可以正式进入 combination freeze`

## 3. 本卡第一步必须先做什么

本卡开启后，第一步不是直接跑组合回放，而是先冻结：

1. 组合对象到底是谁
2. 每个对象当前只允许扮演什么角色
3. 哪些字段明确禁止偷带

也就是说，本卡内部必须先落下：

`formal combination freeze`

## 4. 当前允许进入组合 freeze 的对象

目前只允许从下面已赢下 isolated round 的字段里挑：

1. `duration_percentile`
2. `reversal_state`
3. `context_trend_direction_before`

## 5. 当前显式禁止

当前先写死这些禁止项：

1. 不得把 `wave_role` 偷带进组合
2. 不得把 `current_wave_age_band` 偷升格
3. 不得把 `mirror / conditioning / gene_score` 一起塞进组合
4. 不得在组合卡里偷偷新增还没跑 isolated round 的新字段

## 6. 本卡完成时必须留下什么

1. combination freeze
2. 组合 replay evidence
3. 第一战场 record
4. formal ruling

## 7. 一句话状态

`17.6` 现在已经从“isolated winners 够了但还被 GX8 卡住”，推进到了“GX8 已解除阻塞，因此 Phase 9C 可以正式开工，先做最小组合 freeze”。`
