# Phase 9D / Gene 包级 promotion 裁决卡

- 状态: `Completed`
- 日期: `2026-03-18`
- 类型: `包级 ruling 子阶段`
- 范围: `决定当前 Phase 9 是 promote、retain，还是 defer`

## 1. 这张卡回答什么

这张卡只回答一个问题：

`在 Phase 9A / 9B / 9C 都已经跑到当前边界之后，Gene 包现在到底能 truthfully closeout 成什么结论？`

允许留下的正式输出仍然只有三种：

1. `promote narrow Gene subset`
2. `retain Gene as sidecar only`
3. `defer and open a smaller follow-up package`

---

## 2. 当前输入边界

截至 `2026-03-19`，这张卡真正能消费的正式输入只有：

1. `Phase 9A` 已完成 promoted subset freeze
2. `Phase 9B` 已留下 `3` 个 isolated winners：
   - `duration_percentile`
   - `reversal_state`
   - `context_trend_direction_before`
3. `wave_role` 已完成 isolated validation，但 ruling 是 `retain_sidecar_only`
4. `Phase 9C` 已完成 formal combination freeze
5. `Phase 9C` 当前正式结论仍是：
   `no combination replay opened`
6. 因此当前仍然：
   `Phase 9C has no formal combination winner`

---

## 3. 本轮正式裁决

截至 `2026-03-19`，本卡的正式包级结论改为：

`defer and open a smaller follow-up package`

这意味着：

1. 当前不正式 promotion 任一 Gene 字段进入 default runtime
2. 当前也不把整个 Gene 包退回成“已经研究完、无需继续”
3. 当前包级结论承认：
   `证据不足以 closeout，但足够明确下一步该补什么`

---

## 4. 为什么不是 `promote narrow Gene subset`

这次不能继续把结论写成 `promote narrow Gene subset`，原因很硬：

1. `Phase 9C` 的 `4` 个冻结组合，一个都还没跑正式 combination replay
2. 当前仓库不存在 `Phase 9C` formal combination evidence JSON
3. `duration_percentile` 当前只有：
   - `p95` formal round
   - `p65` sensitivity reference
4. 当前没有：
   - `p70`
   - `p75`
   - `p80`
   - `p85`
   - `p90`
5. 因此当前没有完成：
   `p65 ~ p95, step 5` 的 threshold curve / turning-point / slope-change 判断
6. `duration_percentile` 与 `context_trend_direction_before` 都属于 entry-side 负向过滤面
7. 在没有 duration sweep、也没有 frozen-combination replay 的前提下，直接把 Gene 包写成已 promotion，会把 package closeout 写成缺证据的口头收口

一句话说透：

`当前 isolated winners 已经找到了候选人，但还没有完成你真正关心的寿命轴和趋势轴细化，因此现在还不够资格写包级 promotion。`

---

## 5. 为什么也不是 `retain Gene as sidecar only`

本轮也不能直接把包级结论写成 `retain Gene as sidecar only`。

原因是：

1. `duration_percentile`
2. `reversal_state`
3. `context_trend_direction_before`

这 `3` 个字段都已经赢过 truthful isolated round。

如果现在直接把整包写回 `retain`，等于把下面两类未完成研究一并砍掉：

1. `duration_percentile` 阈值曲线细化
2. `Phase 9C` 冻结组合 replay

这不符合当前真实研究目标：

`先把 Gene 的寿命轴和趋势轴做精，找低频、高胜率、高盈亏比的机会`

---

## 6. 当前真正为真的 runtime 边界

截至 `2026-03-19`，当前主线仍然只能写成：

`legacy_bof_baseline + FIXED_NOTIONAL_CONTROL + FULL_EXIT_CONTROL + Gene sidecar only`

当前没有任何 Gene 字段被本卡 package-promote 进 default runtime。

这也意味着：

1. `reversal_state` 目前仍然只是 isolated winner，不是 package-promoted runtime hook
2. `duration_percentile` 目前仍然只是 isolated winner，不是 package-promoted runtime filter
3. `context_trend_direction_before` 目前仍然只是 isolated winner，不是 package-promoted runtime filter

---

## 7. 本轮 defer 后必须立刻打开的 follow-up

本轮 defer 不是搁置，而是必须立刻打开下面两张更小的 follow-up cards：

1. [`17.8-phase-9e-duration-percentile-threshold-sweep-card-20260319.md`](./17.8-phase-9e-duration-percentile-threshold-sweep-card-20260319.md)
2. [`17.9-phase-9f-frozen-combination-replay-card-20260319.md`](./17.9-phase-9f-frozen-combination-replay-card-20260319.md)

它们的职责分别是：

1. `17.8`
   - 跑 `p65 / p70 / p75 / p80 / p85 / p90 / p95`
   - 找 threshold turning point / slope change
   - 明确 `duration_percentile` 的 truthful runtime candidate 还存不存在
2. `17.9`
   - 严格只消费 `17.6` 已冻结的 `4` 个组合
   - 不允许扩字段
   - 不允许偷带别名
   - 不允许用口头解释补条件

---

## 8. 对 `Phase 9C` 的正式回答

本卡仍然必须保留这一条明文事实：

`Phase 9C has no formal combination winner`

但这次不再把它解释成：

`所以现在可以直接 closeout package`

这次更准确的解释是：

1. `17.6` 只完成了 freeze
2. `17.6` 没有完成 replay
3. 因而当前包级结论只能是：
   `defer，并把 replay 放进更小 follow-up`

---

## 9. Rollback Target

当前 rollback target 继续固定为：

`legacy_bof_baseline + FIXED_NOTIONAL_CONTROL + FULL_EXIT_CONTROL + Gene sidecar only`

因为截至现在，这本来就是当前 truthful runtime。

---

## 10. 下一步

当前真实下一步不再是 `Phase 10`。

当前真实顺序改为：

1. 先执行 [`17.8`](./17.8-phase-9e-duration-percentile-threshold-sweep-card-20260319.md)
2. 再执行 [`17.9`](./17.9-phase-9f-frozen-combination-replay-card-20260319.md)
3. 等这两张卡留下正式 evidence 后，再回到 `Phase 9D` 重新判断：
   - promote
   - retain
   - 或继续更小 defer

`Phase 10` 当前继续保持 `blocked-by-Phase-9`。

---

## 11. 一句话状态

`17.7` 当前轮次已完成，但正式结论不是“Gene 已 promotion”，而是“当前证据不足以 closeout；Phase 9 现在必须回到 Gene refinement first，先补 duration sweep 与 frozen-combination replay，再谈 package promotion”。`
