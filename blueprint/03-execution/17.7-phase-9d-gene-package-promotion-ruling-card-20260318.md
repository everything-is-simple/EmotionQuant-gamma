# Phase 9D / Gene 包级 promotion 裁决卡

- 状态: `Planned`
- 日期: `2026-03-18`
- 类型: `包级 closeout / ruling 子阶段`
- 范围: `Gene mainline integration package 的最终 promotion / retention / defer 裁决`

## 1. 这张卡回答什么

这张卡只回答一个问题：

`在 Phase 9A / 9B / 9C 全部跑完之后，第四战场到底应该留下哪一个包级结论。`

## 2. 允许的包级输出

这张卡最终只能产出三类结论之一：

1. `promote narrow Gene subset`
2. `retain Gene as sidecar only`
3. `defer and open a smaller follow-up package`

不允许出现第四种口头模糊结论。

## 3. 开工门槛

这张卡只有在下面条件满足后才允许转为 `Active`：

1. `Phase 9A` 已完成
2. 所有计划中的 `Phase 9B` isolated round 已完成，或被正式写成 `not opened / non-blocking`
3. 如果 `Phase 9C` 被打开，则它也必须完成
4. [`../../gene/03-execution/19-phase-gx8-three-level-trend-hierarchy-card-20260318.md`](../../gene/03-execution/19-phase-gx8-three-level-trend-hierarchy-card-20260318.md) 已完成或被正式裁定为 `non-blocking`

## 4. 包级裁决必须回答的东西

这张卡不能只写一句“Gene 有价值”。

它必须明确回答：

1. 进入 runtime 的到底是哪几个字段
2. 每个字段当前只允许扮演什么角色
3. 哪些字段继续留在 `sidecar only`
4. 是否存在 `Phase 9C` 组合 winner
5. rollback target 是什么

## 5. 当前一句话状态

`17.7 已经开出来，但它现在只是包级收口占位；在前面的 isolated / combination 卡没跑完之前，它不能提前写结论。`
