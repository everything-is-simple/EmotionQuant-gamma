# Phase 5 Research-Line Migration Package Card

**状态**: `Draft`  
**日期**: `2026-03-15`  
**对象**: `第一战场 / research-line migration package`

---

## 1. 目标

`Phase 5` 只做一件事：

`把第二、第三战场已经正式收官的可迁结论，合法接回第一战场主线 SoT。`

---

## 2. 本卡要回答的问题

1. 哪些研究线结论当前允许迁回主线
2. 哪些只能迁治理边界，不能迁默认参数
3. `legacy_bof_baseline` 当前应如何继续写入主线默认口径
4. 哪些“禁止误用层”必须同步补到主线治理
5. 本轮 migration package 应如何分段收口

---

## 3. 冻结输入

1. `只读 Normandy / N3o closeout record`
2. `只读 Positioning / P9 closeout record`
3. `只读 blueprint 当前主线 baseline / implementation / execution breakdown`
4. `不在本卡新增 replay`
5. `不在本卡偷开 targeted mechanism hypothesis`

---

## 4. 本卡交付物

1. 一张 `Phase 5 opening note`
2. 一组 `mainline migration delta`
3. 一组 `v0.01-plus governance / record patch`
4. 一张 `Phase 5 closeout record`

---

## 5. 固定边界

1. 不把研究线 retained queue 直接写成主线默认参数
2. 不把 `TRAIL_SCALE_OUT_25_75` 写成 canonical control
3. 不把 `Tachibana pilot-pack` 写成完整立花系统
4. 不自动打开 `PX1 / PX2`
5. 不重开 `Normandy / Positioning` 旧卡

---

## 6. 当前建议拆段

1. `Phase 5A / Normandy migration boundary absorption`
2. `Phase 5B / Positioning migration boundary absorption`
3. `Phase 5C / mainline no-fake governance patch`
4. `Phase 5 closeout / research-line migration package record`

---

## 7. 一句话结论

`Phase 5` 不是新研究，而是主线吸收包：把研究线已经证明的边界、控制尺子和负面约束迁回主线，把不该偷升格的东西继续挡住。
