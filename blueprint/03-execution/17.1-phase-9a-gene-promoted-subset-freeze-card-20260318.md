# Phase 9A Gene Promoted Subset Freeze Card

- Status: `Completed`
- Date: `2026-03-18`
- Type: `single-variable freeze sub-phase`
- Scope: `Gene runtime candidate subset freeze`

## 1. Goal

This sub-phase answers one question:

`what is the smallest truthful Gene subset allowed to enter formal isolated runtime validation, without verbally promoting Gene into runtime before evidence exists?`

## 2. Current Validated Baseline

The validated runtime baseline remains:

`legacy_bof_baseline + FIXED_NOTIONAL_CONTROL + FULL_EXIT_CONTROL + Gene sidecar only`

This sub-phase does not change runtime behavior.
It freezes the candidate surface for the next validation round.

## 3. Single-Variable Rule

This sub-phase obeys:

1. [`single-variable-validation-discipline-freeze-20260318.md`](../../docs/spec/common/governance/single-variable-validation-discipline-freeze-20260318.md)
2. [`17-phase-9-gene-mainline-integration-package-card-20260318.md`](./17-phase-9-gene-mainline-integration-package-card-20260318.md)

The frozen rule for this round is:

`Phase 9B may test only one Gene runtime variable`

## 4. Current Working Freeze Scope

### 4.1 Candidate under review

The current Gene runtime candidate under review in `Phase 9A` is:

`duration_percentile`

Its intended runtime role for the next round, if this freeze is confirmed, is:

`negative filter only`

It is not being considered in this round as:

1. sizing overlay
2. exit modulation
3. composite Gene gate
4. multi-field Gene bundle

### 4.2 Shadow-only in this round

The following remain sidecar/shadow only for this round:

1. `current_wave_age_band`
2. `mirror_gene_rank`
3. `primary_ruler_rank`
4. `support_rise_ratio`
5. `conditioning` buckets
6. any composite `gene_score`

### 4.3 Pending candidates, not opened yet

The following remain pending and may not join `Phase 9B`:

1. `wave_role`
2. `reversal_state`
3. `context_trend_direction_before`
4. any future `three-level trend hierarchy` fields from `GX8`

## 5. Why `duration_percentile` Goes First

`Phase 9A` currently reviews `duration_percentile` first because:

1. `G4` already ruled it the current `PRIMARY_RULER`
2. it is the narrowest truthful late-life / exhaustion readout in the current Gene surface
3. it can be tested as a negative filter without immediately rewriting sizing or exit semantics

## 6. Explicitly Forbidden in This Freeze

This sub-phase forbids:

1. `duration_percentile + current_wave_age_band` as a bundled runtime package
2. `duration_percentile + wave_role` in the same validation round
3. any `mirror` or `conditioning` field entering runtime before isolated validation
4. any verbal claim that `Gene` has already been promoted into the default runtime

## 7. Deliverables

This sub-phase is complete only when it leaves:

1. this sub-card
2. [`phase-9a-gene-promoted-subset-freeze-evidence-20260318.md`](./evidence/phase-9a-gene-promoted-subset-freeze-evidence-20260318.md)
3. [`phase-9a-gene-promoted-subset-freeze-record-20260318.md`](./records/phase-9a-gene-promoted-subset-freeze-record-20260318.md)
4. [`v0.01-plus-phase-9a-gene-promoted-subset-freeze-20260318.md`](../../docs/spec/v0.01-plus/governance/v0.01-plus-phase-9a-gene-promoted-subset-freeze-20260318.md)
5. [`v0.01-plus-phase-9a-gene-promoted-subset-freeze-20260318.md`](../../docs/spec/v0.01-plus/records/v0.01-plus-phase-9a-gene-promoted-subset-freeze-20260318.md)

## 8. Acceptance Criteria

1. exactly one Gene runtime candidate is opened
2. the validated baseline remains unchanged
3. evidence exists for why this candidate is the first one
4. conclusion exists for what is frozen, what is excluded, and what remains pending
5. all other Gene fields are explicitly classified as `shadow-only`, `pending`, or `forbidden`
6. the next round can answer, without ambiguity:
   `what exact single Gene variable is under test?`

## 9. Completed Result

`Phase 9A` is now completed.

What is already true:

1. the working freeze direction has narrowed to `duration_percentile`
2. the intended validation role is `negative filter only`
3. the baseline remains `legacy_bof_baseline + FIXED_NOTIONAL_CONTROL + FULL_EXIT_CONTROL + Gene sidecar only`
4. evidence and conclusion have now been written down in first-battlefield execution space

What is still not true:

1. `Gene` has not yet entered default runtime
2. `Phase 9B` has not yet run
3. no combination package may open yet

## 10. Next Step

The next allowed step is:

`Phase 9B / isolated runtime validation of duration_percentile as negative filter, alone`
