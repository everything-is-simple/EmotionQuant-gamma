# Phase 4.1 Card

**状态**: `Active`  
**日期**: `2026-03-11`  
**对象**: `P4.1 MSS / Broker 整改`  
**定位**: `当前主线第六张执行卡`  
**上游锚点**:

1. `blueprint/02-implementation-spec/01-current-mainline-implementation-spec-20260308.md`
2. `blueprint/03-execution/01-current-mainline-execution-breakdown-20260308.md`
3. `blueprint/03-execution/06-phase-4-gate-card-20260309.md`
4. `docs/spec/v0.01-plus/records/v0.01-plus-phase-4-gate-decision-20260311.md`
5. `blueprint/01-full-design/04-mss-lite-contract-annex-20260308.md`
6. `blueprint/01-full-design/05-broker-risk-contract-annex-20260308.md`
7. `blueprint/01-full-design/08-mss-minimal-tradable-design-20260309.md`
8. `blueprint/01-full-design/09-mainline-system-operating-baseline-20260309.md`

---

## 1. 目标

在不推翻 `Phase 3 exit` 和 `Phase 4 NO-GO` 的前提下，专门拆解并整改：

`MSS -> Broker 执行层`

当前卡只服务一件事：

`把当前 NO-GO 主因拆清、冻结一条可重跑的整改候选路径，并准备重新进入 Gate。`

---

## 2. 范围

当前 phase 只做：

1. `max_positions / risk_per_trade_pct / max_position_pct` 三条容量来源拆解
2. `RISK_OFF -> 后续 RISK_ON` 路径反噬拆解
3. 一条 `MSS / Broker` 整改候选路径冻结
4. 整改候选相对“当前 NO-GO 主链”的 replay、幂等与归因复核
5. 重新进入 `Phase 4 Gate` 前的证据打包准备

当前 phase 明确不做：

1. 不重开 `Phase 3`
2. 不改写 `Phase 4 = NO-GO`
3. 不提前翻 `PAS_PATTERNS` 默认
4. 不把 `IRS baseline` 升格为本卡第一整改对象
5. 不允许同时跨 `PAS / IRS / MSS / Broker` 四层一起调参

### 2.1 当前卡和 `P3 / P4` 的边界

1. `Phase 3` 已完成：
   - `risk_regime` 已正式落库
   - Broker 已稳定消费 `risk_regime`
2. `Phase 4` 已完成：
   - 当前正式裁决为 `NO-GO`
   - rollback target 已冻结为 `legacy_bof_baseline`
3. `Phase 4.1` 不是 `Phase 3.9`
   - 它不补 `P3` 的尾巴
   - 它是 `P4 NO-GO` 之后的专项整改段

### 2.2 当前卡冻结的整改边界

当前卡默认先承认下面 4 个事实，不在本卡里反复争论：

1. `IRS` 排序层不是当前主嫌疑
2. 当前问题主链路主要落在 `MSS -> Broker`
3. `MSS` 不是所有场景都纯负面
4. 当前正式默认短窗主链相对 `legacy_bof_baseline` 仍然失败

因此本卡优先回答：

1. 到底是哪条容量杠杆伤害最大
2. `RISK_OFF` 当天减损和后续 `RISK_ON` 路径反噬分别贡献了什么
3. 应该先修哪一条执行杠杆，而不是先动 `PAS`

### 2.3 当前卡继承的固定运行纪律

本卡默认继承：

`blueprint/03-execution/00-current-dev-data-baseline-20260311.md`

因此默认固定为：

1. 正式执行库：
   - `G:\EmotionQuant_data\emotionquant.duckdb`
2. 旧库候选：
   - `G:\EmotionQuant_data\duckdb\emotionquant.duckdb`
3. working copy / 临时缓存：
   - `G:\EmotionQuant-temp`
4. 补数顺序：
   - 先看 `RAW_DB_PATH / 本地旧库`
   - 再走 `TUSHARE_PRIMARY_*`
   - `TUSHARE_FALLBACK_*` 只做失败兜底

### 2.4 当前执行进度

截至 `2026-03-11`：

1. `P4.1-A / capacity decomposition` 已完成
2. `P4.1-B / path dependency attribution` 已完成
3. `P4.1-C / remediation candidate freeze` 已完成
4. `P4.1-D / Gate re-entry bundle` 已完成
5. 已明确：
   - `max_positions` 是当前第一主嫌疑
   - `risk_per_trade_pct / max_position_pct` 当前主要只改 `quantity`
   - 当前 BOF 默认路径没有出现 `delayed entry` 补回
   - 主因链条已收口为 `max_positions shrink + position carryover + slot scarcity`
   - `carryover_buffer(1)` 已冻结为当前唯一整改候选
   - `carryover_buffer(1)` 已完成 `Gate re-entry bundle`
   - 当前裁决是“允许重新进入 `Phase 4 Gate replay`，但不构成直接 `GO`”
6. 后续 replay 结果已明确：
   - `Phase 4 Gate replay` 已完成
   - `carryover_buffer(1)` 仍未通过 `legacy` 对照 Gate
   - 当前正式默认路径继续保持 `legacy_bof_baseline`
7. 当前治理裁决已固定：
   - 继续 `Phase 4.1`
   - 但只允许再冻结 `1` 条最终候选
   - 不允许再沿 `carryover_buffer(2/3/...)` 做局部微调
8. `P4.1-E / final candidate freeze` 已完成：
   - 当前最终候选固定为 `size_only_overlay`
   - 语义固定为：`MSS` 保留 regime 判断，但 Broker 不再消费 `max_positions shrink`
   - 当前结果已证明它相对 `current_full_overlay_hard_cap` 同时改善 `EV / PF / MDD`
9. `P4.1-F / final direct Gate replay` 已完成：
   - 比较对象固定为 `legacy_bof_baseline` 与 `size_only_overlay`
   - `size_only_overlay` 仍未推翻 `legacy_bof_baseline`
10. 当前卡已按约束直接关闭：
   - `Phase 4.1` 不再冻结新的 `MSS / Broker` 候选
   - 正式默认路径继续保持 `legacy_bof_baseline`

---

## 3. 任务

### 3.1 Task P4.1-A Capacity Source Decomposition

**当前状态**

1. `completed`
2. 记录：
   - `docs/spec/v0.01-plus/records/v0.01-plus-phase-4.1-capacity-decomposition-20260311.md`
3. evidence：
   - `docs/spec/v0.01-plus/evidence/mss_capacity_decomposition_dtt_v0_01_dtt_pattern_plus_irs_mss_score_w20260105_20260224_t_phase4_1_a__mss_capacity_decomposition.json`

**代码落点**

1. `src/broker/risk.py`
2. `src/broker/broker.py`
3. `src/config.py`

**测试落点**

1. `tests/unit/broker/test_broker.py`
2. `tests/integration/backtest/test_backtest_engine.py`

**脚本落点**

1. `scripts/backtest/run_v001_plus_mss_capacity_decomposition.py`

**证据落点**

1. `docs/spec/v0.01-plus/evidence/`
2. `docs/spec/v0.01-plus/records/`

**至少覆盖**

1. 单独隔离：
   - `max_positions`
   - `risk_per_trade_pct`
   - `max_position_pct`
2. 再补一组组合重放：
   - `current full overlay`
   - `only max_positions`
   - `only risk_per_trade`
   - `only max_position_pct`
3. 每组都必须稳定给出：
   - `trade_count`
   - `reject_rate`
   - `participation_rate`
   - `skip_maxpos_count`
   - `trade_set_changed_count`
   - `quantity_changed_count`
4. 不能只看汇总收益，必须同时保留 `decision_bucket / overlay_reason / risk_regime`

### 3.2 Task P4.1-B Path Dependency / Backfire Attribution

**脚本落点**

1. `scripts/backtest/run_v001_plus_mss_path_dependency_attribution.py`

**证据落点**

1. `docs/spec/v0.01-plus/evidence/`
2. `docs/spec/v0.01-plus/records/`

**至少覆盖**

1. 当前 focus dates 固定先从：
   - `2026-01-30`
   - `2026-02-02`
   - `2026-02-06`
   - `2026-02-09`
   - `2026-02-24`
2. sizing-only 对照日固定补看：
   - `2026-01-13`
   - `2026-01-20`
   - `2026-01-29`
   - `2026-02-04`
   - `2026-02-05`
3. 必须能回答每个变化是：
   - `RISK_OFF 当天拒单`
   - `RISK_OFF 当天缩量`
   - `后续 RISK_ON 新增成交`
   - `路径改写后的持仓竞争`
4. 不能只列“新增了哪些票”，必须区分：
   - `slot freed`
   - `position carried`
   - `delayed entry`
   - `quantity resize`
5. 结果必须能回连：
   - `signal_id`
   - `run_id`
   - `signal_date`
   - `execute_date`
   - `risk_regime`
   - `decision_bucket`

### 3.3 Task P4.1-C Remediation Candidate Freeze

**当前状态**

1. `completed`
2. 记录：
   - `docs/spec/v0.01-plus/records/v0.01-plus-phase-4.1-remediation-candidate-freeze-20260311.md`
3. evidence：
   - `docs/spec/v0.01-plus/evidence/mss_remediation_candidate_freeze_dtt_v0_01_dtt_pattern_plus_irs_mss_score_w20260105_20260224_t_phase4_1_c__mss_remediation_candidate_freeze.json`

**代码落点**

1. `src/broker/risk.py`
2. `src/config.py`
3. `src/broker/broker.py`

**测试落点**

1. `tests/unit/broker/test_broker.py`
2. `tests/integration/backtest/test_backtest_engine.py`

**执行约束**

1. 当前卡只允许冻结：
   - 一条 `MSS / Broker` 整改候选
2. 不允许出现“改一点 `PAS`，再改一点 `IRS`，再改一点 `MSS`”的混合口径
3. 候选必须显式落到：
   - 脚本参数
   - 配置项
   - records 说明
   不能只藏在临时命令或本地 `.env`
4. 整改后 trace 语义不能退化：
   - `regime_source`
   - `overlay_reason`
   - `decision_bucket`
   - `effective_*`
   必须继续稳定可追溯

**本轮冻结结果**

1. 当前唯一整改候选固定为：
   - `MSS_MAX_POSITIONS_MODE=carryover_buffer`
   - `MSS_MAX_POSITIONS_BUFFER_SLOTS=1`
2. 当前不允许并行冻结第二条 `MSS / Broker` 候选
3. 当前不允许在 `P4.1-C` 内重新开启 `risk_per_trade / max_position_pct / PAS / IRS`

### 3.4 Task P4.1-D Gate Re-entry Bundle

**当前状态**

1. `completed`
2. 记录：
   - `docs/spec/v0.01-plus/records/v0.01-plus-phase-4.1-gate-reentry-bundle-20260311.md`
3. evidence：
   - `docs/spec/v0.01-plus/evidence/matrix_summary_dtt_v0_01_dtt_pattern_plus_irs_mss_score_w20260105_20260224_t_phase4_1_d_current__dtt_matrix.json`
   - `docs/spec/v0.01-plus/evidence/matrix_summary_dtt_v0_01_dtt_pattern_plus_irs_mss_score_carryover_buffer1_w20260105_20260224_t_phase4_1_d_candidate__dtt_matrix.json`
   - `docs/spec/v0.01-plus/evidence/idempotency_dtt_v0_01_dtt_pattern_plus_irs_mss_score_carryover_buffer1_w20260105_20260224_t_phase4_1_d__idempotency_check.json`
   - `docs/spec/v0.01-plus/evidence/trade_attribution_dtt_v0_01_dtt_pattern_plus_irs_mss_score_vs_v0_01_dtt_pattern_plus_irs_mss_score_carryover_buffer1_w20260105_20260224_t_phase4_1_d__trade_attribution.json`
   - `docs/spec/v0.01-plus/evidence/windowed_sensitivity_dtt_v0_01_dtt_pattern_plus_irs_mss_score_vs_v0_01_dtt_pattern_plus_irs_mss_score_carryover_buffer1_w20251222_20260224_t_phase4_1_d__windowed_sensitivity.json`
4. 运行约束：
   - 只承认重建窗口内 `L3` 之后的 replay 结果
   - `--skip-rebuild-l3` 产生的漂移结果不计入本卡裁决

**脚本落点**

1. `scripts/backtest/run_v001_plus_dtt_matrix.py`
2. `scripts/backtest/check_idempotency.py`
3. `scripts/backtest/run_v001_plus_trade_attribution.py`
4. `scripts/backtest/run_v001_plus_windowed_sensitivity.py`

**文档落点**

1. `docs/spec/common/records/development-status.md`
2. `docs/spec/v0.01-plus/records/`

**检查项**

1. 整改候选相对当前 `NO-GO` 主链，必须重新给出：
   - matrix replay
   - idempotency
   - attribution bundle
2. `Phase 4.1` 自己不宣布系统级 `GO`
3. `Phase 4.1` 只负责回答：
   - 是否具备“重新进入 Gate”的资格

**当前 re-entry 对象固定为**

1. `current_full_overlay_hard_cap`
2. `candidate_full_overlay_carryover_buffer`

**当前 bundle 裁决**

1. `current_full_overlay_hard_cap` 继续保持 `NO-GO` 对照对象
2. `candidate_full_overlay_carryover_buffer` 已取得重新进入 `Phase 4 Gate replay` 的资格
3. 当前不允许把 `re-entry eligible` 解释成直接 `GO`
4. 后续 direct `Phase 4 Gate replay` 已确认：该候选相对 `legacy_bof_baseline` 仍然 `NO-GO`

### 3.5 Task P4.1-E Final Candidate Freeze

**当前状态**

1. `completed`
2. 记录：
   - `docs/spec/v0.01-plus/records/v0.01-plus-phase-4.1-size-only-candidate-freeze-20260311.md`
3. evidence：
   - `docs/spec/v0.01-plus/evidence/mss_remediation_candidate_freeze_dtt_v0_01_dtt_pattern_plus_irs_mss_score_size_only_overlay_w20260105_20260224_t_phase4_1_e__mss_remediation_candidate_freeze.json`

**治理裁决**

1. 当前允许继续 `Phase 4.1`
2. 但只允许再冻结这 `1` 条最终候选
3. 当前明确禁止：
   - `carryover_buffer(2/3/...)`
   - 再开第二条 `MSS / Broker` 候选
   - 重开 `PAS / IRS`

**当前冻结对象**

1. DTT variant：
   - `v0_01_dtt_pattern_plus_irs_mss_score_size_only_overlay`
2. Broker mode：
   - `MSS_MAX_POSITIONS_MODE=no_maxpos_shrink`
3. 运行语义：
   - 保留 `risk_regime`
   - 保留 `risk_per_trade_pct / max_position_pct` 缩放
   - 不再让 Broker 消费 `max_positions shrink`

**本轮结果**

1. 相对 `current_full_overlay_hard_cap`：
   - `trade_count: 9 -> 16`
   - `EV: -0.008988 -> -0.003532`
   - `PF: 0.4613 -> 0.4968`
   - `MDD: 0.006035 -> 0.009241`
   - `buy_reject_maxpos_count: 7 -> 0`
2. 相对 `baseline_no_overlay`：
   - `trade_set_changed_count = 0`
   - `quantity_changed_count = 14`
   - `reject_set_changed_count = 0`
3. 约束性结论：
   - 这条候选已经把 `slot shrink` 从 Broker 执行面剥离干净
   - 当前差异主要退化成 `MSS sizing-only overlay`

**下一步**

1. 只允许带着这条最终候选进入最后一次 direct `Phase 4 Gate replay`
2. 如果它仍然不能推翻 `legacy_bof_baseline`，则直接关闭 `Phase 4.1`

### 3.6 Task P4.1-F Final Direct Gate Replay

**当前状态**

1. `completed`
2. 记录：
   - `docs/spec/v0.01-plus/records/v0.01-plus-phase-4-gate-replay-size-only-overlay-20260311.md`
3. evidence：
   - `docs/spec/v0.01-plus/evidence/matrix_summary_dtt_phase4_gate_replay_legacy_vs_size_only_overlay_w20260105_20260224_t_phase4_replay__dtt_matrix.json`
   - `docs/spec/v0.01-plus/evidence/idempotency_legacy_legacy_bof_baseline_w20260105_20260224_t_phase4_gate_replay_size_only__idempotency_check.json`
   - `docs/spec/v0.01-plus/evidence/idempotency_dtt_v0_01_dtt_pattern_plus_irs_mss_score_size_only_overlay_w20260105_20260224_t_phase4_gate_replay_size_only__idempotency_check.json`
   - `docs/spec/v0.01-plus/evidence/trade_attribution_legacy_bof_baseline_vs_v0_01_dtt_pattern_plus_irs_mss_score_size_only_overlay_w20260105_20260224_t_phase4_gate_replay_size_only__trade_attribution.json`
   - `docs/spec/v0.01-plus/evidence/rank_decomposition_legacy_bof_baseline_to_v0_01_dtt_pattern_plus_irs_mss_score_size_only_overlay_w20260105_20260224_t_phase4_gate_replay_size_only__rank_decomposition.json`
   - `docs/spec/v0.01-plus/evidence/windowed_sensitivity_legacy_bof_baseline_vs_v0_01_dtt_pattern_plus_irs_mss_score_size_only_overlay_w20251222_20260224_t_phase4_gate_replay_size_only__windowed_sensitivity.json`

**当前 replay 对象固定为**

1. `legacy_bof_baseline`
2. `v0_01_dtt_pattern_plus_irs_mss_score_size_only_overlay`

**脚本落点**

1. `scripts/backtest/run_v001_plus_dtt_matrix.py`
2. `scripts/backtest/check_idempotency.py`
3. `scripts/backtest/run_v001_plus_trade_attribution.py`
4. `scripts/backtest/run_v001_plus_rank_decomposition.py`
5. `scripts/backtest/run_v001_plus_windowed_sensitivity.py`

**至少覆盖**

1. 短窗 direct matrix replay：
   - `2026-01-05` 到 `2026-02-24`
2. 双边 idempotency：
   - `legacy_bof_baseline`
   - `size_only_overlay`
3. direct attribution bundle：
   - `trade attribution`
   - `rank decomposition`
   - `windowed sensitivity`
4. 结果必须明确回答：
   - `size_only_overlay` 能否推翻 `legacy_bof_baseline`
   - 若不能，失败主因仍落在 `MSS sizing overlay` 还是其它路径

**约束性裁决**

1. 这是 `Phase 4.1` 允许执行的最后一个任务
2. 如果 replay 仍然 `NO-GO`：
   - 直接结束 `Phase 4.1`
   - 不再冻结新的 `MSS / Broker` 候选

**当前 replay 结果**

1. 短窗 direct matrix replay 已完成：
   - `legacy_bof_baseline = trade_count 5 / EV 0.010740 / PF 2.2327 / MDD 0.003867`
   - `size_only_overlay = trade_count 16 / EV -0.003532 / PF 0.4968 / MDD 0.009241`
2. 双边 `idempotency` 已通过：
   - `metrics_equal = true`
   - 差距不是重放噪音
3. formal `top50_pos10` direct attribution 已明确：
   - `net_delta(candidate - legacy) = -9043.4152`
   - `MAX_POSITIONS` 拒单集合变化已归零
   - 差异主因已从 `slot shrink` 转成 `trade_set + quantity` 路径
4. 更长窗口 `windowed sensitivity` 结果为 mixed：
   - tighter capacity 场景下 `candidate` 可优于 `legacy`
   - 但 `top50_pos10` 仍不能稳定推翻 `legacy`
5. 当前卡的关闭条件已触发：
   - `Phase 4.1 = completed`
   - 默认路径继续保持 `legacy_bof_baseline`

---

## 4. 出场条件

- [x] 已明确哪条容量杠杆是当前主嫌疑
- [x] 已明确动态缩容 + carryover 的主链条（当前 BOF 默认路径未见 `delayed entry` 补回）
- [x] 至少 1 条整改候选已冻结
- [x] 整改候选相对当前 `v0_01_dtt_pattern_plus_irs_mss_score` 满足 `expected_value >= current`
- [x] 整改候选相对当前 `v0_01_dtt_pattern_plus_irs_mss_score` 满足 `profit_factor >= current`
- [ ] 整改候选相对当前 `v0_01_dtt_pattern_plus_irs_mss_score` 满足 `max_drawdown <= current`
- [x] 整改候选已通过 `check_idempotency.py`
- [x] rollback path 仍可执行
- [x] 已明确是否允许重新进入 `Phase 4 Gate`
- [x] 已完成 `Phase 4 Gate replay`
- [ ] replay 后候选相对 `legacy_bof_baseline` 满足 `GO` 阈值

---

## 5. 完成后必须回答的问题

1. `max_positions / risk_per_trade_pct / max_position_pct` 里谁伤害最大
2. `RISK_OFF` 的减损贡献和 `RISK_ON` 的路径反噬，分别有多大
3. 当前应该先修执行层哪一条杠杆，而不是先翻 `PAS` 默认
4. 冻结后的整改候选到底是什么
5. 这条整改候选是否值得重新进入 `Phase 4 Gate`
