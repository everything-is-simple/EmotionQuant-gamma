# Phase 9B Isolated Duration Percentile Validation Card

- Status: `Completed`
- Date: `2026-03-18`
- Type: `single-variable runtime validation sub-phase`
- Scope: `duration_percentile as negative filter, alone`

## 1. Goal

This sub-phase answers one question:

`if duration_percentile alone is connected into the current mainline as a negative filter, while everything else stays fixed at the validated baseline, does the runtime result become better or only more complicated?`

## 2. Current Validated Baseline

The validated runtime baseline remains:

`legacy_bof_baseline + FIXED_NOTIONAL_CONTROL + FULL_EXIT_CONTROL + Gene sidecar only`

`Phase 9B` does not re-open:

1. `IRS-lite / MSS-lite`
2. Gene sizing overlays
3. Gene exit modulation
4. any multi-field Gene package

## 3. Hard Dependency

This sub-phase depends on:

1. [`16-phase-8-data-contract-residual-audit-card-20260318.md`](./16-phase-8-data-contract-residual-audit-card-20260318.md)
2. [`17.1-phase-9a-gene-promoted-subset-freeze-card-20260318.md`](./17.1-phase-9a-gene-promoted-subset-freeze-card-20260318.md)
3. [`phase-9a-gene-promoted-subset-freeze-evidence-20260318.md`](./evidence/phase-9a-gene-promoted-subset-freeze-evidence-20260318.md)
4. [`phase-9a-gene-promoted-subset-freeze-record-20260318.md`](./records/phase-9a-gene-promoted-subset-freeze-record-20260318.md)
5. [`single-variable-validation-discipline-freeze-20260318.md`](../../docs/spec/common/governance/single-variable-validation-discipline-freeze-20260318.md)

Without all five conditions, `Phase 9B` may not claim truthful runtime validation.

## 4. Validation Boundary

`Phase 9B` may change only one thing:

`add duration_percentile negative-filter semantics to the validated baseline`

That means:

1. entry backbone remains `legacy_bof_baseline`
2. position sizing remains `FIXED_NOTIONAL_CONTROL`
3. exit semantics remain `FULL_EXIT_CONTROL`
4. Gene remains sidecar everywhere except this single negative-filter hook

Explicitly forbidden in this round:

1. `duration_percentile + current_wave_age_band`
2. `duration_percentile + wave_role`
3. `duration_percentile + reversal_state`
4. any `mirror` companion field
5. any `conditioning` companion field
6. any Gene-driven sizing or exit branch

## 5. Candidate Semantics Under Test

The only runtime candidate under test is:

`duration_percentile`

The only allowed runtime role under test is:

`negative filter only`

This sub-phase must answer three concrete questions:

1. what exact `duration_percentile` block rule is being tested
2. how many otherwise-valid signals it removes
3. whether the isolated removal improves the validated baseline enough to justify keeping the rule

## 6. Required Outputs

This sub-phase is complete only when it leaves:

1. this sub-card
2. one `Phase 9B` evidence file under `blueprint/03-execution/evidence/`
3. one `Phase 9B` record under `blueprint/03-execution/records/`
4. one formal archive record under `docs/spec/v0.01-plus/records/`
5. an explicit ruling:
   - `promote_duration_percentile_negative_filter`
   - `retain_sidecar_only`
   - `defer_with_smaller_follow_up`

## 7. Acceptance Criteria

1. only one Gene runtime variable is changed relative to the validated baseline
2. the tested rule is fully codified and traceable
3. blocked-signal count and blocked-signal share are written down
4. replay outcome versus baseline is written down
5. no unstated human interpretation leaks into runtime
6. rollback to `legacy_bof_baseline + FIXED_NOTIONAL_CONTROL + FULL_EXIT_CONTROL + Gene sidecar only` remains trivial

## 8. Next Step After This Card

If this isolated round wins truthfully, only then may the package ask whether another single Gene field deserves its own round.

If this isolated round fails, the truthful next state is:

`retain Gene as sidecar only and do not verbally promote duration_percentile`

## 9. Executed Rule

The executed runtime rule in this round was:

`block when current_wave_duration_percentile >= 95`

No other Gene field joined the rule.

## 10. Real Output

This completed round leaves:

1. [`phase-9b-duration-percentile-validation-evidence-20260318.md`](./evidence/phase-9b-duration-percentile-validation-evidence-20260318.md)
2. [`phase-9b-duration-percentile-validation-record-20260318.md`](./records/phase-9b-duration-percentile-validation-record-20260318.md)
3. [`v0.01-plus-phase-9b-duration-percentile-validation-20260318.md`](../../docs/spec/v0.01-plus/records/v0.01-plus-phase-9b-duration-percentile-validation-20260318.md)
4. [`phase9b_duration_percentile_validation_legacy_duration_percentile_negative_filter_p95_w20260105_20260224_t085921__phase9_duration_validation.json`](../../docs/spec/v0.01-plus/evidence/phase9b_duration_percentile_validation_legacy_duration_percentile_negative_filter_p95_w20260105_20260224_t085921__phase9_duration_validation.json)

## 11. Final Ruling

This round closes with:

`promote_duration_percentile_negative_filter`

Why:

1. `6 / 16` formal signals were blocked, or `37.5%`
2. all `6` blocked signals were real baseline `BUY filled` entries
3. full-window replay improved versus baseline on `expected_value`, `profit_factor`, and `max_drawdown`

What this ruling does not claim:

1. full `Gene` runtime promotion is complete
2. combination testing is now open
3. default runtime has already changed
