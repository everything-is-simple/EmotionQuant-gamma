# 第四战场卡目录

**状态**: `Active`  
**日期**: `2026-03-16`

---

## 1. 结论

第四战场完整卡体系固定为 `11` 张：

1. 必选主线卡 `9` 张：`G0 ~ G8`
2. 可选条件卡 `2` 张：`GX1 ~ GX2`

当前状态：

1. 已完成：`G0 / G1 / G2`
2. 已开未做：`G3 ~ G8`
3. 条件卡待触发：`GX1 / GX2`

---

## 2. 必选主线卡

1. `G0 / wave-ruler opening scaffold`
   - 作用：搭对象层脚手架与正式表合同
   - 状态：`Completed`
2. `G1 / factor attribution baseline`
   - 作用：先回答 `magnitude / duration / extreme_density` 谁更硬
   - 状态：`Completed`
3. `G2 / percentile band calibration`
   - 作用：把 `65 / 95` 做成历史分布带，而不是新参数森林
   - 状态：`Completed`
4. `G3 / structure label calibration`
   - 作用：把 `1-2-3 / 2B` 从描述词升级成正式结构标签
   - 状态：`Opened`
5. `G4 / self-history ruler validation`
   - 作用：验证“个股自历史标尺”到底是否真有解释力
   - 状态：`Opened`
6. `G5 / market-industry-index mirror ruler`
   - 作用：把个股历史尺镜像到指数、行业和大盘
   - 状态：`Opened`
7. `G6 / BOF-PB-CPB conditioning readout`
   - 作用：把第四战场标签回灌到 Normandy 条件层
   - 状态：`Opened`
8. `G7 / MSS-IRS refactor-or-retire decision`
   - 作用：基于第四战场结果，对旧体系做改造或退役决策
   - 状态：`Opened`
9. `G8 / gene campaign closeout`
   - 作用：冻结第四战场最终交付与边界
   - 状态：`Opened`

---

## 3. 可选条件卡

1. `GX1 / targeted detector rewrite`
   - 触发条件：现有 `pivot / turn / 2B` 检测器的一致性不足，已阻塞 `G3`
   - 状态：`Opened`
2. `GX2 / targeted migration package`
   - 触发条件：第四战场结论需要集中迁移旧表、旧脚本或旧文档入口
   - 状态：`Opened`

---

## 4. 固定顺序

第四战场默认推进顺序固定为：

1. `G0 -> G1 -> G2 -> G3 -> G4 -> G5 -> G6 -> G7 -> G8`

条件卡只允许在触发条件满足时插入：

1. `GX1` 只能插在 `G3` 前后
2. `GX2` 只能插在 `G5` 或 `G7` 前后

---

## 5. 使用原则

1. `G0 ~ G8` 是主线，不因短期实现难度而跳卡
2. `GX1 / GX2` 不是默认必做项，不得抢主线编号
3. 每张卡都只回答一个问题，不重新变回“全系统大而全总设计”
4. 所有实现都必须沿用 `gene/01-full-design -> 02-implementation-spec -> 03-execution` 三层目录
