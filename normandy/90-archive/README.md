# Normandy Archive

## 定位

这里存 `Normandy` 研究战役启动早期的方案、草案和已退场计划。

它们不代表当前 `Normandy` SoT，只保留：

1. 启动时的思路
2. 后来被更正式的 `README / implementation-spec / execution records` 替代的方案
3. 仍值得回看的研究起点

## 当前文件

| 文件 | 角色 |
|---|---|
| `v0.01-alpha-provenance-execution-plan-20260311.md` | `Normandy` 启动早期的执行计划归档 |
