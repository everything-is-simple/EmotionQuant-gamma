# Normandy Full Design

**状态**: `Active`  
**日期**: `2026-03-12`

---

## 1. 定位

`01-full-design/` 是 `Normandy` 第二战场的完整设计 SoT。

它不再只是预留层。

截至 `2026-03-12`，`BOF baseline`、`FB` 候选地位、`SB` 的 refinement 边界以及 `Tachibana` 的 observation-only 裁决都已经有了正式 record。

因此这里当前必须承载：

1. 第二战场的开发宗旨
2. 第二战场的架构思想
3. baseline / promotion queue / no-go queue
4. 候选回迁主线的合法升格法

---

## 2. 当前使用规则

当前阶段固定为：

1. 先看 `01-alpha-first-mainline-charter-20260312.md`，确认第二战场的主线宗旨、架构和升格法。
2. 再看 `02-implementation-spec/`，确认当前 active spec 与实验边界。
3. 再看 `03-execution/`，确认当前执行卡、证据和阶段 record。
4. `Normandy` 专属 research asset 继续统一收进 `90-research-assets/`。

---

## 3. 当前入口

- `01-alpha-first-mainline-charter-20260312.md`
- `90-research-assets/README.md`

---

## 4. 当前一句话定位

`这里已经从占位层切换为正式设计层：它负责把 Normandy 的 alpha-first 研究主线、对象裁决和升格法写死。`
