# Phase N3N Tachibana Pilot-Pack Migration Boundary Card

**日期**: `2026-03-15`  
**阶段**: `Normandy / N3n`  
**对象**: `Tachibana pilot-pack migration boundary`  
**状态**: `Draft`

---

## 1. 目标

`N3n` 只做一件事：

`把当前 pilot-pack 的阶段性战果压成 migration boundary。`

---

## 2. 本卡要回答的问题

1. 当前 Tachibana pilot-pack 到底正式确认了什么
2. 哪些结论可以迁回主线，哪些只能留在研究线
3. 哪些属于治理边界与负面约束，哪些绝不能误写成默认参数
4. 当前 retained 的到底是 `execution boundary` 还是 `new system`
5. `N3o / campaign closeout` 应怎样排队

---

## 3. 冻结输入

1. `只读 N3g ~ N3m formal records`
2. `只读 current matrix / digest / sidecar evidence`
3. `不在本卡新增 replay`
4. `pilot-pack != full Tachibana system`

---

## 4. 本卡交付件

1. 一张 `pilot-pack migration boundary table`
2. 一张 `migration note to mainline`
3. 一张 `formal migration-boundary record`

---

## 5. 固定边界

1. 不把 `TRAIL_SCALE_OUT_25_75` 写成主线正式语义替代
2. 不把 `CD10` 写成默认 cooldown
3. 不把 `reduced_unit_scale` 写成已落地 sizing
4. 不把 `single_lot floor sanity` 写成第二 operating lane

---

## 6. 下一步出口

1. `N3o / Tachibana pilot-pack campaign closeout`

---

## 7. 一句话结论

`N3n` 要做的不是继续找新花样，而是把“能迁什么、不能迁什么”钉死。
