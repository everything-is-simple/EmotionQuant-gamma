# Phase N3M Tachibana Pilot-Pack Experimental-Segment Isolation Card

**日期**: `2026-03-15`  
**阶段**: `Normandy / N3m`  
**对象**: `Tachibana pilot-pack experimental-segment isolation`  
**状态**: `Draft`

---

## 1. 目标

`N3m` 只做一件事：

`把 pilot-pack 里的 experimental 读数和 canonical aggregate 彻底分开。`

---

## 2. 本卡要回答的问题

1. `experimental_segment_policy = isolate_from_canonical_aggregate` 到底要怎么落成正式口径
2. 哪些 pilot 结果必须只作为 `experimental sidecar`
3. 哪些 aggregate 明确禁止并入 `canonical control/proxy` 主结论
4. `matrix / digest / record` 三层各自应该怎样写隔离语义
5. `N3n / migration boundary` 应以什么前提开工

---

## 3. 冻结输入

1. `只读 N3g ~ N3l formal records`
2. `只读当前 pilot-pack matrix / digest payload`
3. `不在本卡重新打开 cooldown / unit-regime 争论`
4. `E4 = governance overlay only`

---

## 4. 本卡交付件

1. 一张 `experimental-segment isolation note`
2. 一张 `experimental sidecar digest boundary`
3. 一张 `formal isolation record`

---

## 5. 固定边界

1. 不把 `experimental_100_share` 并入 canonical aggregate
2. 不因为隔离语义而重写 runner
3. 不把 sidecar 结果误写成默认主结论
4. 不借本卡偷开 `R2 / R3 / R8 / R9`

---

## 6. 下一步出口

1. `N3n / Tachibana pilot-pack migration boundary`

---

## 7. 一句话结论

`N3m` 的职责不是再跑新实验，而是先把“哪些结果只能旁路展示、不能混入主结论”写死。`
