# v0.01-plus Phase 9A Gene Promoted Subset Freeze

**Status**: `Completed`  
**Phase**: `Phase 9A / Gene promoted subset freeze`  
**Date**: `2026-03-18`

---

## 1. Position

This file does not promote `Gene` into runtime.

It freezes the smallest truthful Gene subset that is allowed to attempt isolated runtime validation.

One-line conclusion:

`Phase 9A opens exactly one Gene runtime candidate: duration_percentile as negative filter only.`

---

## 2. Current Validated Baseline

The baseline remains:

`legacy_bof_baseline + FIXED_NOTIONAL_CONTROL + FULL_EXIT_CONTROL + Gene sidecar only`

`mainline_default_runtime_changed = no`

---

## 2.1 Evidence Chain

This freeze is grounded on:

1. [`../../../../blueprint/03-execution/evidence/phase-9a-gene-promoted-subset-freeze-evidence-20260318.md`](../../../../blueprint/03-execution/evidence/phase-9a-gene-promoted-subset-freeze-evidence-20260318.md)
2. [`../../../../blueprint/03-execution/records/phase-9a-gene-promoted-subset-freeze-record-20260318.md`](../../../../blueprint/03-execution/records/phase-9a-gene-promoted-subset-freeze-record-20260318.md)

---

## 3. Freeze Matrix

| Gene surface | Current runtime role | Phase 9A freeze result | Why |
|---|---|---|---|
| `duration_percentile` | sidecar ruler | `single-variable candidate` | `G4 PRIMARY_RULER`; narrowest truthful late-life readout |
| `current_wave_age_band` | sidecar summary | `shadow-only` | derivative of `duration_percentile`; excluded to avoid double-counting in round one |
| `wave_role` | sidecar structural context | `pending candidate` | just repaired by `GX4`; not allowed to join first isolated runtime round |
| `reversal_state` | sidecar reversal context | `pending candidate` | still separate from first runtime test |
| `context_trend_direction_before` | sidecar parent-context field | `pending candidate` | not yet opened for runtime use |
| `mirror_gene_rank` | dashboard / attribution | `shadow-only` | market and industry mirror remain non-default runtime surface |
| `primary_ruler_rank` | dashboard / attribution | `shadow-only` | cannot silently become direct runtime gate |
| `support_rise_ratio` | dashboard / attribution | `shadow-only` | environment descriptor only in this round |
| `conditioning` buckets | attribution | `shadow-only` | not opened as direct runtime filter |
| composite `gene_score` | composite summary | `forbidden` | composite hard gate remains untruthful as default runtime object |

---

## 4. Frozen Boundary for the Next Round

`Phase 9B` may test only:

`duration_percentile -> negative filter semantics`

`Phase 9B` may not test:

1. `duration_percentile` together with `current_wave_age_band`
2. `duration_percentile` together with `wave_role`
3. any `mirror` or `conditioning` field as a runtime companion
4. any Gene-driven sizing or exit modulation

---

## 5. What This Freeze Does Not Claim

This freeze does **not** claim:

1. that `duration_percentile` already improved runtime
2. that `Gene` has already entered the default system
3. that other Gene fields are invalid forever
4. that combination packages may now open

It claims only:

`the first truthful runtime candidate is duration_percentile, alone`

---

## 6. Next Allowed Move

The next allowed move is:

`Phase 9B / isolated replay of duration_percentile as negative filter, alone`

Combination testing remains forbidden until later isolated rounds finish and receive formal records.
