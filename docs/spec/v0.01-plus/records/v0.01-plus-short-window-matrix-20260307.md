# v0.01-plus 短窗矩阵与幂等记录（2026-03-07）

- **范围**: `2026-01-05` 至 `2026-02-24`
- **目标**: 验证 `v0.01-plus` 主线四组固定矩阵可重跑，并补齐默认 DTT 主线的幂等检查
- **数据源**: `G:\EmotionQuant_data\emotionquant.duckdb`
- **工作副本**: `G:\EmotionQuant-temp\backtest\dtt-matrix-20260307.duckdb`, `G:\EmotionQuant-temp\backtest\idempotency-20260307.duckdb`
- **证据**:
  - `docs/spec/v0.01-plus/evidence/matrix_summary_dtt_v0_01_dtt_bof_plus_irs_score_w20260105_20260224_t160708__dtt_matrix.json`
  - `docs/spec/v0.01-plus/evidence/idempotency_dtt_v0_01_dtt_bof_plus_irs_score_w20260105_20260224_t151843__idempotency_check.json`
  - `docs/spec/v0.01-plus/evidence/coverage_audit_dtt_v0_01_dtt_bof_plus_irs_score_w20260210_20260213_t163939__coverage_audit.json`
  - `docs/spec/v0.01-plus/evidence/execution_sensitivity_dtt_v0_01_dtt_bof_only_vs_v0_01_dtt_bof_plus_irs_score_w20260105_20260224_t162521__execution_sensitivity.json`
  - `docs/spec/v0.01-plus/records/v0.01-plus-coverage-and-rank-audit-20260307.md`

## 1. 矩阵结果摘要

| variant | trade_count | signals_count | ranked_signals_count | profit_factor | expected_value | max_drawdown |
|---|---:|---:|---:|---:|---:|---:|
| `legacy_bof_baseline` | 7 | 7 | 0 | 0.2897 | -0.0199 | 0.0115 |
| `v0_01_dtt_bof_only` | 16 | 16 | 16 | 0.4968 | -0.0035 | 0.0183 |
| `v0_01_dtt_bof_plus_irs_score` | 16 | 16 | 16 | 0.4968 | -0.0035 | 0.0176 |
| `v0_01_dtt_bof_plus_irs_mss_score` | 16 | 16 | 16 | 0.4968 | -0.0035 | 0.0176 |

## 2. 直接结论

1. raw 与 execution 分区修复后，四组固定矩阵已在同一数据快照、同一成本模型下重新跑通，`legacy` 与三组 `DTT` 仍可稳定重跑。
2. `legacy_bof_baseline` 与 `DTT` 仍被明显切开：`legacy` 仅产生 `7` 笔交易，而三组 `DTT` 都产生 `16` 笔交易。
3. 修复覆盖异常后，`DTT` 三组的聚合指标仍保持一致：说明 `IRS/MSS` 的差异没有直接体现在短窗总收益表上，但这不再是由 `31` 行 raw 截断造成的。
4. 新的敏感性证据表明：`IRS` 的排序变化已经进入执行约束。
5. 默认主线 `v0_01_dtt_bof_plus_irs_score` 的幂等验证已通过：两次运行的订单集、成交集与汇总指标完全一致。

## 3. 覆盖修复结论

| 日期 | raw `raw_daily` | execution `l1_stock_daily` | execution `l2_stock_adj_daily` |
|---|---:|---:|---:|
| `2026-02-10` | 5470 | 5470 | 5470 |
| `2026-02-11` | 5470 | 5470 | 5470 |
| `2026-02-13` | 5474 | 5474 | 5474 |

直接判断：

1. `2026-02-10 / 02-11 / 02-13` 的 `31` 行异常已经消失，raw 源库与执行库在焦点日期重新对齐。
2. 修复后 `selector` 在这些日期的 `universe` 回到 `5470 / 5470 / 5474`，说明前一轮异常确实是上游数据截断，不是 `selector` 误杀。
3. 因此，后续再出现收益或排序异常，优先检查 `Top-N / max_positions / 资金占用顺序`，而不是继续怀疑 raw 覆盖。

## 4. 执行约束敏感性

| 场景 | 结论 | 关键变化 |
|---|---|---|
| `top1_pos1` | `IRS` 已进入 Broker 风控边界 | `selected_changed=8`，`MAX_POSITIONS` 拒单集合变化 `8` 次 |
| `top1_pos2` | `IRS` 已进入实际成交集合 | `trade_set_changed=4`，变化发生在 `2026-01-20`、`2026-02-04` |
| `top2_pos1` | `IRS` 已进入 Broker 风控边界 | `selected_changed=4`，`MAX_POSITIONS` 拒单集合变化 `4` 次 |
| `top2_pos2` | `IRS` 已进入实际成交集合 | `trade_set_changed=4`，变化发生在 `2026-01-20`、`2026-02-04` |
| `top50_pos10` | `IRS` 已进入实际仓位数量 | `quantity_changed=9`，变化发生在 `2026-01-20`、`2026-01-30`、`2026-02-04`、`2026-02-05` |

直接结论：

1. 之前“`IRS` 只改名次、不改执行”的判断只对旧的宽松解释成立，现在已经被敏感性矩阵推翻。
2. 在收紧 `Top-N / max_positions` 后，`IRS` 会改变 `Strategy` 入选、`Broker` 的 `MAX_POSITIONS` 拒单集合，以及实际 `BUY` 成交集合。
3. 即使在 `top50_pos10` 的宽场景下，`IRS` 也会通过同日买入顺序改变现金预占路径，从而改变多个信号的最终买入数量。
4. `MSS` 在当前短窗里仍然没有独立证据证明其带来了横截面排序增益。

## 5. 当前 Gate 判断

### 已满足

- `PG1` ~ `PG7`
- `EG1`
- `EG2`
- `EG4`

### 已显著去风险但暂不勾选

- `EG3`：raw/execution 覆盖异常已修复，且 `IRS` 已证明确实会进入执行约束；但仍需补齐多窗口稳定性与失败模式解释。

### 尚未满足

- `EG5`：尚未完成七维评审与失败模式说明。
- `EG6`：暂不下 `GO / NO-GO` 最终结论。

## 6. 发现的问题

1. 修复 raw 覆盖后，`DTT` 三组的聚合指标仍完全一致，说明当前短窗里 `IRS/MSS` 的收益差异还没有在总收益层形成可见优势。
2. `IRS` 已经改变实际成交集合与仓位数量，但这种变化是否能稳定转化为收益提升，还需要更长窗口或更多切片来验证。
3. `MSS` 在该窗口内没有形成横截面排序作用，暂时只表现为分数平移项。

## 7. 下一步建议

1. 把敏感性矩阵从短窗扩到更长窗口，验证 `IRS` 对成交集合和仓位数量的影响是否稳定。
2. 补一份 `IRS` 交易归因拆解，重点看 `2026-01-20 / 01-30 / 02-04 / 02-05` 这 4 个执行日的数量变化是否改善盈亏。
3. 在完成多窗口稳定性与失败模式解释前，不下默认切换 `GO` 结论。
