# v0.01-plus Phase 7 Data Provider Refactor Closeout (2026-03-18)

## 1. Package

- Package: `Phase 7 / data provider refactor`
- Scope: `data ingress + local data contract cutover`
- Decision: `completed`

## 2. What Phase 7 changed

Phase 7 formally replaced the old shared data posture:

`TuShare-first + old raw db reuse`

with the new truthful posture:

`TDX local-first + BaoStock light incremental + TuShare emergency fallback`

This change affected the first battlefield because it rewired the shared system spine:

`raw -> L1 -> L2`

## 3. What was promoted

The following are now formally promoted into the truthful current mainline data boundary:

1. `vipdoc` as the historical raw base
2. `T0002/hq_cache + mootdx` as the local static-asset ingress
3. `BaoStock` as light incremental patch/fallback only
4. `TuShare` as emergency fallback only
5. `l1_industry_member` as the active industry contract
6. local `up_limit / down_limit` derivation on `l1_stock_daily`

## 4. What was downgraded

The following were formally downgraded from active default ownership:

1. `TuShare-first` daily ingress
2. `raw_limit_list` as a default-path dependency
3. `SW2021-only` active runtime industry semantics
4. `raw_daily_basic` as a primary runtime dependency

## 5. What Phase 7 did not claim

This closeout does not claim:

1. that local generic industry buckets are a perfect historical SW archive
2. that all historical exchange-rule corner cases are now modeled forever
3. that BaoStock or TuShare are removed from the repo
4. that future data-contract changes no longer need a formal package

## 6. Formal Evidence

Supporting closeout evidence:

1. `docs/spec/v0.01-plus/records/v0.01-plus-phase-7b-local-data-contract-regression-audit-20260318.md`
2. `docs/reference/a-stock-rules/a-share-local-price-limit-runtime-rules-20260318.md`

Observed verification:

1. raw database refreshed to `20260317`
2. execution `L1` refreshed to `2026-03-17`
3. `pytest = 237 passed`
4. `preflight = passed`

## 7. Final Ruling

`Phase 7` is now formally closed with:

1. `phase_7a_completed = yes`
2. `phase_7b_completed = yes`
3. `active_data_provider_posture = TDX local-first`
4. `active_industry_contract = l1_industry_member`
5. `active_price_limit_contract = local_rule_derived`
6. `future_data_contract_change_requires = new_formal_package`

## 8. One-line Conclusion

`Phase 7` closed successfully: the mainline now runs on a truthful local-first data base, with online providers reduced to patch and emergency roles.
