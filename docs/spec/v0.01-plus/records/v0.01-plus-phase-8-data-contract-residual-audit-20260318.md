# v0.01-plus Phase 8 Data Contract Residual Audit

**Status**: `Completed`  
**Date**: `2026-03-18`

---

## 1. Question

`after Phase 7 cut the system over to TDX local-first data posture, which old data-contract assumptions still remain in active code, tests, docs, and runtime explanations?`

---

## 2. Current Validated Baseline

The validated runtime baseline used for this audit is:

`legacy_bof_baseline + FIXED_NOTIONAL_CONTROL + FULL_EXIT_CONTROL + Gene sidecar only`

The validated data posture used for this audit is:

`TDX local-first + BaoStock light incremental + TuShare emergency fallback`

---

## 3. Audit Scope

This audit reviewed:

1. `src/data`
2. `src/selector`
3. `src/broker`
4. `src/backtest`
5. `tests/unit/data`
6. `tests/unit/selector`
7. `blueprint/`
8. `docs/reference/`
9. `docs/spec/v0.01-plus/`

---

## 4. Final Conclusion

`Phase 7` did not leave behind a hidden runtime split-brain.

The active runtime contract is now stable enough for later isolated promotion work, but a small set of compatibility and historical residuals still remains.

Formal conclusion:

`completed_with_documented_compatibility_residuals`

---

## 5. What Is Now Formally True

The audit confirms the following active truths:

1. active data-provider posture = `TDX local-first`
2. active industry contract = `industry_member / l1_industry_member`
3. active price-limit contract = local rule derivation on `l1_stock_daily`
4. active runtime no longer depends on online `stk_limit`
5. active runtime no longer requires strict `SW2021-only` industry semantics
6. `raw_daily_basic` is no longer a primary runtime dependency

---

## 6. Residual Findings

### 6.1 Resolved during Phase 8

1. `IRS trace` still labeled active runtime industry classification as `SW2021`
2. This was corrected to `INDUSTRY_MEMBER_RUNTIME`
3. Affected runtime surfaces:
   - `src/selector/irs.py`
   - `src/strategy/ranker.py` via imported constant

### 6.2 Accepted compatibility residuals

These residuals remain, but are classified as compatibility or history, not active-runtime truth:

1. `src/data/fetcher.py::TuShareFetcher.fetch_industry_members()`
   - still fetches `SW2021 L1` remote members
   - accepted as fallback compatibility, not active runtime SoT
2. `src/data/sw_industry.py`
   - still keeps `normalize_sw_l1_classify()`, `build_sw_l1_*()` and `build_l1_sw_industry_member_rows()`
   - accepted as migration/historical wrapper layer
3. `raw_daily_basic`
   - still exists in raw schema, scripts, tests, and some joins
   - accepted as compatibility table, not active runtime driver
4. `AKShareFetcher`
   - still exists as dormant compatibility skeleton
   - not part of the active data posture

### 6.3 Document residuals

The following still preserve older SW-centric IRS wording and should be treated as design/reference residuals, not live runtime truth:

1. `blueprint/01-full-design/03-irs-lite-contract-annex-20260308.md`
2. `docs/Strategy/IRS/shenwan-industry-classification.md`

These are not allowed to override:

1. `Phase 7` closeout
2. current runtime data posture
3. active `industry_member / l1_industry_member` contract

### 6.4 Historical residuals explicitly allowed

The following remain untouched on purpose:

1. `docs/spec/v0.01/records/*`
2. `docs/spec/v0.01/evidence/*`
3. old TuShare/SW31 historical decisions and evidence

These remain as historical fact, not active contract.

---

## 7. Ruling

The audit rules the current system as:

1. `truthful enough for isolated runtime experiments`
2. `not blocked by hidden active data-contract split`
3. `still carrying documented compatibility shells`

Therefore:

1. `Phase 8` is completed
2. `Phase 9` is unblocked from a data-contract perspective
3. `Phase 10` is unblocked from a data-contract perspective

This does **not** mean:

1. `Gene` is automatically promoted
2. lifecycle changes may skip isolated validation
3. old IRS/SW documentation is now the runtime truth

---

## 8. Validation

This audit round validated with:

1. repository-wide residual search over `SW2021 / l1_sw_industry_member / raw_daily_basic / stk_limit / provider posture`
2. `powershell -ExecutionPolicy Bypass -File scripts/ops/preflight.ps1 -Profile hook`

Result:

`preflight = passed`
