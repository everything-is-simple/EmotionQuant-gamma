# v0.01-plus Phase 9A Gene Promoted Subset Freeze

**Status**: `Completed`  
**Date**: `2026-03-18`

---

## 1. Question

`after Phase 8 cleared the active data-contract floor, what is the smallest truthful Gene subset allowed to enter runtime validation first?`

---

## 2. Current Validated Baseline

The validated baseline remains:

`legacy_bof_baseline + FIXED_NOTIONAL_CONTROL + FULL_EXIT_CONTROL + Gene sidecar only`

This round changed:

`no runtime behavior`

This round froze:

`the first isolated Gene runtime candidate`

Formal first-battlefield evidence and conclusion now exist at:

1. [`../../../../blueprint/03-execution/evidence/phase-9a-gene-promoted-subset-freeze-evidence-20260318.md`](../../../../blueprint/03-execution/evidence/phase-9a-gene-promoted-subset-freeze-evidence-20260318.md)
2. [`../../../../blueprint/03-execution/records/phase-9a-gene-promoted-subset-freeze-record-20260318.md`](../../../../blueprint/03-execution/records/phase-9a-gene-promoted-subset-freeze-record-20260318.md)

---

## 3. Ruling

Formal ruling:

`Phase 9A completed with single-field candidate freeze`

The only opened Gene runtime candidate is:

`duration_percentile`

Its allowed role in the next round is:

`negative filter only`

The following remain outside the round:

1. `current_wave_age_band`
2. `wave_role`
3. `reversal_state`
4. `context_trend_direction_before`
5. all `mirror` fields
6. all `conditioning` buckets
7. any composite `gene_score`
8. any Gene-driven sizing or exit modulation

---

## 4. Why This Is the First Candidate

The freeze chooses `duration_percentile` first because:

1. `G4` already established it as the current `PRIMARY_RULER`
2. it is the most defensible late-life / exhaustion variable in the existing Gene surface
3. it can be tested without prematurely turning Gene into a multi-switch runtime boss

---

## 5. Runtime Truth After Phase 9A

What is true now:

1. `Gene` is still sidecar only in live runtime
2. `Phase 9B` is now formally allowed to test `duration_percentile` alone
3. no other Gene field may join that round

What is not true now:

1. `Gene` has not yet been promoted
2. `Phase 9B` has not yet run
3. no combined Gene runtime package exists

---

## 6. Next Step

The next formal step is:

`Phase 9B / isolated runtime validation of duration_percentile as negative filter, alone`

Rollback target remains:

`legacy_bof_baseline + FIXED_NOTIONAL_CONTROL + FULL_EXIT_CONTROL + Gene sidecar only`
