# v0.01-plus Phase 4 Gate 正式裁决与 Rollback 条件（2026-03-11）

- **阶段**: `Phase 4 / Gate`
- **结论**: `NO-GO`
- **正式裁决对象**:
  - `legacy_bof_baseline`
  - `v0_01_dtt_pattern_plus_irs_mss_score`
- **正式裁决结果**:
  - 当前 `v0.01-plus` 主链不升级为正式默认运行路径
  - `legacy_bof_baseline` 继续保留为正式回退目标
  - `v0.01-plus` 保留为整改/实验路径，不删除既有 evidence

## 1. 本轮证据

1. `docs/spec/v0.01-plus/evidence/matrix_summary_dtt_phase4_legacy_vs_mainline_bof_w20260105_20260224_t_legacy_gate__dtt_matrix.json`
2. `docs/spec/v0.01-plus/evidence/idempotency_dtt_v0_01_dtt_pattern_plus_irs_mss_score_w20260105_20260224_t_phase4_gate__idempotency_check.json`
3. `docs/spec/v0.01-plus/evidence/windowed_sensitivity_dtt_v0_01_dtt_pattern_plus_irs_score_vs_v0_01_dtt_pattern_plus_irs_mss_score_w20251222_20260224_t_phase4_gate__windowed_sensitivity.json`
4. `docs/spec/v0.01-plus/evidence/trade_attribution_dtt_v0_01_dtt_pattern_plus_irs_score_vs_v0_01_dtt_pattern_plus_irs_mss_score_w20260105_20260224_t_phase4_focus__trade_attribution.json`
5. `docs/spec/v0.01-plus/evidence/trade_attribution_focus_digest_dtt_v0_01_dtt_pattern_plus_irs_score_vs_v0_01_dtt_pattern_plus_irs_mss_score_w20260105_20260224_t_phase4_focus__trade_digest.json`
6. `docs/spec/v0.01-plus/evidence/rank_decomposition_dtt_v0_01_dtt_pattern_plus_irs_score_to_v0_01_dtt_pattern_plus_irs_mss_score_w20260105_20260224_t_phase4_ytc5__rank_decomposition.json`
7. `docs/spec/v0.01-plus/evidence/mss_regime_sensitivity_dtt_v0_01_dtt_pattern_plus_irs_mss_score_w20260105_20260224_t020505__mss_regime_sensitivity.json`

## 2. Gate Bundle 收口结果

### 2.1 Matrix Replay

正式比较窗口固定为：`2026-01-05` 至 `2026-02-24`

| 场景 | trade_count | EV | PF | MDD | reject_rate | participation_rate |
|---|---:|---:|---:|---:|---:|---:|
| `legacy_bof_baseline` | `5` | `0.010740` | `2.2327` | `0.003867` | `0.0000` | `1.0000` |
| `v0_01_dtt_pattern_plus_irs_mss_score` | `9` | `-0.008988` | `0.4613` | `0.006035` | `0.2800` | `0.5625` |

相对 `legacy_bof_baseline`：

1. `expected_value` 更低：`-0.008988 < 0.010740`
2. `profit_factor` 更低：`0.4613 < 0.95 * 2.2327`
3. `max_drawdown` 更高：`0.006035 > 1.10 * 0.003867`

### 2.2 Idempotency

`check_idempotency.py` 本轮先修正了两处口径缺口，再正式取证：

1. 增加显式 `--pipeline-mode / --dtt-variant`，避免继续沿用 `.env` 中的旧默认 variant
2. 默认先重建当前窗口 `L3`，与 `Matrix Replay` 的窗口语义对齐

修正后的正式结果：

1. `order_id_set_equal = true`
2. `trade_id_set_equal = true`
3. `metrics_equal = true`
4. 两次运行都与正式矩阵一致：
   - `trade_count = 9`
   - `expected_value = -0.008988`
   - `profit_factor = 0.461303`
   - `max_drawdown = 0.006035`
   - `skip_maxpos_count = 7`

结论：`幂等通过，但通过后的对象仍然是 NO-GO 对象。`

### 2.3 Attribution Bundle

本卡冻结要求的四类归因证据现已齐全：

1. `trade attribution`
2. `windowed sensitivity`
3. `rank decomposition`
4. `regime sensitivity`

并且当前可以解释主要收益变化来源：

1. `rank_changed_count` 在关键对照里持续为 `0`
2. 主要变化发生在 `trade_set / quantity / MAX_POSITIONS reject set`
3. 根因主要落在 `MSS -> Broker` 执行层，而不是 `IRS` 排序层

## 3. Windowed Sensitivity 的补充结论

`windowed_sensitivity` 对比对象固定为：

1. `v0_01_dtt_pattern_plus_irs_score`
2. `v0_01_dtt_pattern_plus_irs_mss_score`

窗口固定为：`2025-12-22` 至 `2026-02-24`

五个场景全部完成，`failed_count = 0`。共同结论：

1. 五个场景的 `rank_changed_count = 0`
2. 五个场景的 `selected_changed_count = 0`
3. 变化依然主要落在 `trade_set / quantity / maxpos reject`

场景差异：

1. `top1_pos1` / `top2_pos1`
   - 成交集合不变
   - 只发生数量变化
   - `MSS` 让 `MDD` 进一步下降
2. `top1_pos2` / `top2_pos2`
   - `MSS` 通过额外 `2` 条 `MAX_POSITIONS` 拒单，把成交从 `5` 压到 `3`
   - 同时改善了 `EV / PF / MDD`
3. `top50_pos10`
   - `trade_count: 17 -> 13`
   - `EV: -0.006605 -> -0.004365`
   - `PF: 0.4955 -> 0.4825`
   - `MDD: 0.023021 -> 0.011098`

约束性结论：

`MSS 不是所有场景都纯负面。它在更紧的容量约束下能减损，甚至改善 EV/PF；但这不足以推翻正式 Gate 对当前默认路径相对 legacy 的 NO-GO。`

## 4. 正式裁决

按 `blueprint/03-execution/06-phase-4-gate-card-20260309.md` 的冻结规则，当前已命中 `NO-GO`：

1. `Matrix Replay` 已完成
2. `check_idempotency.py` 已通过
3. attribution bundle 已齐全并可解释主要变化来源
4. 但相对 `legacy_bof_baseline`：
   - `expected_value < legacy`
   - 且 `profit_factor < 0.95 * legacy`
5. 同时：
   - `max_drawdown > 1.10 * legacy`

因此正式写定：

`Phase 4 / Gate = NO-GO`

## 5. Rollback Condition（冻结）

### 5.1 回退目标

`legacy_bof_baseline`

### 5.2 触发条件

命中下面任一条，即维持或执行回退：

1. 任一 `NO-GO` 条件命中
2. 或主线证据不能解释收益变化

本轮已实际命中：

1. `expected_value < legacy`
2. `profit_factor < 0.95 * legacy`
3. `max_drawdown > 1.10 * legacy`

### 5.3 回退后动作

1. 不把当前 `v0.01-plus` 主链提升为正式默认运行路径
2. `legacy_bof_baseline` 继续保持可重跑、可比较
3. `v0.01-plus` 继续保留为整改路径，不删除：
   - `PAS / MSS` 归因证据
   - `legacy` 对照证据
   - `windowed sensitivity / idempotency` 证据

## 6. 当前根因判断与整改顺序

当前正式结论不是“`MSS` 一无是处”，也不是“只要把 `PAS` 扩成五形态就能解决”，而是：

1. 当前默认短窗主链相对 `legacy` 仍然失败
2. `IRS` 排序层不是这轮主嫌疑
3. 主要变化发生在 `MSS -> Broker` 的容量执行层
4. 早期 `RISK_OFF` 减损与后续 `RISK_ON` 路径反噬共同构成当前失败链条

下一步整改顺序固定为：

1. 先拆 `MSS / Broker` 容量来源与路径反噬
2. 再决定 `PAS` 默认是否扩容
3. `IRS baseline` 继续保留为治理项，但不作为当前第一整改优先级

## 7. 运行纪律回写

1. 正式源库仍固定为：`G:\EmotionQuant_data\emotionquant.duckdb`
2. working db / 临时副本固定落：`G:\EmotionQuant-temp\backtest`
3. evidence 固定回写：`docs/spec/v0.01-plus/evidence/`
4. 本轮未触发补数；双 TuShare key 规则保持 blueprint 基线：
   - 先看 `RAW_DB_PATH / 本地旧库`
   - 再走 `TUSHARE_PRIMARY_*`
   - `TUSHARE_FALLBACK_*` 只做失败兜底
