# v0.01-plus Phase 4 Gate Replay（carryover_buffer(1)）记录（2026-03-11）

- **阶段**: `Phase 4 / Gate replay`
- **结论**: `NO-GO persists`
- **正式裁决对象**:
  - `legacy_bof_baseline`
  - `v0_01_dtt_pattern_plus_irs_mss_score_carryover_buffer1`
- **正式裁决结果**:
  - `carryover_buffer(1)` 已完成 direct Gate replay
  - 当前 replay 结果仍未推翻 `legacy_bof_baseline`
  - 默认运行路径继续保持 `legacy_bof_baseline`

## 1. 本轮证据

1. `docs/spec/v0.01-plus/evidence/matrix_summary_dtt_phase4_gate_replay_legacy_vs_carryover_buffer1_w20260105_20260224_t_phase4_replay__dtt_matrix.json`
2. `docs/spec/v0.01-plus/evidence/idempotency_legacy_legacy_bof_baseline_w20260105_20260224_t_phase4_gate_replay__idempotency_check.json`
3. `docs/spec/v0.01-plus/evidence/idempotency_dtt_v0_01_dtt_pattern_plus_irs_mss_score_carryover_buffer1_w20260105_20260224_t_phase4_gate_replay__idempotency_check.json`
4. `docs/spec/v0.01-plus/evidence/trade_attribution_legacy_bof_baseline_vs_v0_01_dtt_pattern_plus_irs_mss_score_carryover_buffer1_w20260105_20260224_t_phase4_gate_replay__trade_attribution.json`
5. `docs/spec/v0.01-plus/evidence/rank_decomposition_legacy_bof_baseline_to_v0_01_dtt_pattern_plus_irs_mss_score_carryover_buffer1_w20260105_20260224_t_phase4_gate_replay__rank_decomposition.json`
6. `docs/spec/v0.01-plus/evidence/windowed_sensitivity_legacy_bof_baseline_vs_v0_01_dtt_pattern_plus_irs_mss_score_carryover_buffer1_w20251222_20260224_t_phase4_gate_replay__windowed_sensitivity.json`
7. `docs/spec/v0.01-plus/records/v0.01-plus-phase-4.1-gate-reentry-bundle-20260311.md`

## 2. 运行口径

1. 正式短窗 Gate replay 固定为：`2026-01-05` 至 `2026-02-24`
2. 更长窗口 sensitivity 固定为：`2025-12-22` 至 `2026-02-24`
3. 比较对象固定为：
   - `legacy_bof_baseline`
   - `v0_01_dtt_pattern_plus_irs_mss_score_carryover_buffer1`
4. 正式源库固定为：`G:\EmotionQuant_data\emotionquant.duckdb`
5. working copy / artefact cache 固定落：`G:\EmotionQuant-temp`
6. evidence 固定回写：`docs/spec/v0.01-plus/evidence/`
7. 本轮继续固定三目录纪律与双 TuShare key 规则：
   - 先看 `RAW_DB_PATH / 本地旧库`
   - 再走 `TUSHARE_PRIMARY_*`
   - `TUSHARE_FALLBACK_*` 只做失败兜底

## 3. Gate Replay 结果

### 3.1 Matrix Replay

| 场景 | trade_count | EV | PF | MDD | reject_rate | participation_rate |
|---|---:|---:|---:|---:|---:|---:|
| `legacy_bof_baseline` | `5` | `0.010740` | `2.2327` | `0.003867` | `0.0000` | `1.0000` |
| `v0_01_dtt_pattern_plus_irs_mss_score_carryover_buffer1` | `13` | `-0.000116` | `0.6213` | `0.007427` | `0.1034` | `0.8125` |

相对 `legacy_bof_baseline`：

1. `expected_value` 更低：`-0.000116 < 0.010740`
2. `profit_factor` 更低：`0.6213 < 0.95 * 2.2327`
3. `max_drawdown` 更高：`0.007427 > 1.10 * 0.003867`

约束性结论：

`carryover_buffer(1)` 虽优于旧的 current hard-cap 路径，但在正式 Gate replay 里仍然直接命中 NO-GO 阈值。`

### 3.2 Idempotency

本轮直接把两侧正式比较对象都补了幂等：

1. `legacy_bof_baseline`
   - `metrics_equal = true`
   - `trade_count = 5`
   - `expected_value = 0.010740`
   - `profit_factor = 2.232724`
   - `max_drawdown = 0.003867`
2. `carryover_buffer(1)`
   - `metrics_equal = true`
   - `trade_count = 13`
   - `expected_value = -0.000116`
   - `profit_factor = 0.621349`
   - `max_drawdown = 0.007427`

约束性结论：

`这次 replay 的差距不是幂等噪音，而是稳定可重放的对象差异。`

### 3.3 Direct Trade Attribution

focus execute dates 固定为：

1. `2026-01-06`
2. `2026-01-12`
3. `2026-01-23`
4. `2026-01-30`
5. `2026-02-02`
6. `2026-02-04`
7. `2026-02-05`
8. `2026-02-06`
9. `2026-02-09`
10. `2026-02-24`

其中有 `8` 个执行日出现实质变化。

聚合后：

1. `legacy_changed_entry_pnl_total = +5239.4632`
2. `candidate_changed_entry_pnl_total = -2010.8118`
3. `net_delta(candidate - legacy) = -7250.2750`

最关键的变化日：

1. `2026-01-12`
   - legacy 独有 `002475`
   - `entry_pnl = -3888.9608`
   - 候选在这一天避免了这笔亏损
2. `2026-01-30`
   - legacy 独有 `600887`：`-1381.4039`
   - 候选独有 `600436`：`+411.5929`
   - 候选在这一天也更好
3. `2026-02-02`
   - 候选独有 `300014`
   - `entry_pnl = -2664.8278`
4. `2026-02-05`
   - 候选独有 `300308`
   - 同时 `002175 / 002547` 数量路径被改写
   - 当日 legacy 变化样本合计 `+183.7069`
   - 候选变化样本合计 `-4638.4097`
5. `2026-02-06`
   - 两侧都成交 `002384`
   - 但 legacy `1300` 股，候选只有 `500` 股
   - `entry_pnl: +10326.1210 -> +3971.5850`

约束性结论：

`candidate` 并不是“全程都错”；它确实修掉了部分坏交易。但 direct replay 下最关键的收益缺口，集中出现在 `2026-02-02 / 2026-02-05 / 2026-02-06` 这组执行日，最终净效果仍明显落后于 legacy。`

### 3.4 Direct Rank / Execution Decomposition

短窗 direct rank decomposition 的核心事实是：

1. `legacy_bof_baseline` 没有 `DTT rank sidecar`
2. `shared_signal_count = 0`
3. `right_only_count = 16`
4. `buy_trade_impact.trade_set_changed_count = 12`
5. `buy_trade_impact.quantity_changed_count = 3`
6. `maxpos_reject_impact.reject_set_changed_count = 3`

约束性结论：

`legacy -> candidate` 的 direct replay 不是“同一套 rank frame 小修小补”，而是完整 pipeline 语义和执行约束一起换了对象。因此本轮 rank decomposition 的主要价值是证明：差异主要落在执行集合和数量路径，而不是共享 rank 内部的名次洗牌。`

### 3.5 Windowed Sensitivity

`completed_count = 5`，`failed_count = 0`。

共同结论：

1. direct replay 在更长窗口下不是单边结论
2. candidate 在 tighter capacity 场景下会优于 legacy
3. 但在 `top50_pos10` 这种更接近正式默认的场景下，仍然是 mixed，而不是压倒性胜出

代表性结果：

1. `top1_pos1`
   - `legacy EV = -0.037475`
   - `candidate EV = 0.023198`
   - `candidate PF / MDD` 都更好
2. `top1_pos2`
   - `legacy EV = -0.031602`
   - `candidate EV = 0.014443`
   - `candidate PF / MDD` 都更好
3. `top50_pos10`
   - `legacy EV = -0.016841`
   - `candidate EV = -0.009953`
   - `legacy PF = 0.9142`
   - `candidate PF = 0.6375`
   - `legacy MDD = 0.015721`
   - `candidate MDD = 0.011685`

约束性结论：

`更长窗口 direct replay 呈现 mixed outcome：candidate 在 EV/MDD 上有时更好，但 PF 并不稳定占优。这不足以推翻短窗 Gate replay 对 legacy 的正式 NO-GO 结论。`

## 4. 正式裁决

按 `blueprint/03-execution/06-phase-4-gate-card-20260309.md` 的冻结标准，本轮继续命中 `NO-GO`：

1. `Matrix Replay` 已完成
2. `legacy / candidate` 双边 `idempotency` 已通过
3. direct attribution bundle 已补齐
4. 但相对 `legacy_bof_baseline`：
   - `expected_value < legacy`
   - 且 `profit_factor < 0.95 * legacy`
   - 同时 `max_drawdown > 1.10 * legacy`

因此本轮正式裁决为：

`Phase 4 Gate replay (carryover_buffer(1)) = NO-GO persists`

## 5. Rollback Condition（维持不变）

1. 正式回退目标继续保持：
   - `legacy_bof_baseline`
2. 当前默认路径不切到：
   - `v0_01_dtt_pattern_plus_irs_mss_score_carryover_buffer1`
3. `Phase 4.1` 产生的 candidate evidence 保留，但不升级为正式默认

## 6. 本轮结论

1. `carryover_buffer(1)` 已完成 direct Gate replay
2. 它相对旧的 current hard-cap path 仍然是有效整改候选
3. 但相对 `legacy_bof_baseline`，当前 replay 结果仍然不足以翻转 `NO-GO`
4. 当前正式默认路径保持不变：
   - `legacy_bof_baseline`
