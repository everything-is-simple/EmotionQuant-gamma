# v0.01-plus Phase 8 Data Contract Remediation Checklist

**Status**: `Open`  
**Date**: `2026-03-18`

This checklist records residuals found by:

[`v0.01-plus-phase-8-data-contract-residual-audit-20260318.md`](./v0.01-plus-phase-8-data-contract-residual-audit-20260318.md)

---

## 1. Compatibility Residuals Kept On Purpose

1. `src/data/fetcher.py::TuShareFetcher.fetch_industry_members()`
   - keep as fallback compatibility
   - do not present as active runtime contract
2. `src/data/sw_industry.py` SW-only wrapper helpers
   - keep as migration/history compatibility
   - do not reopen unless IRS runtime package needs narrower cleanup
3. `raw_daily_basic`
   - keep as compatibility table and report support
   - do not treat as active runtime hard dependency
4. `AKShareFetcher`
   - keep only as dormant structural compatibility
   - do not present as active provider posture

---

## 2. Documentation Residuals

These files should be cleaned only in a focused IRS documentation cleanup, not during unrelated runtime packages:

1. `blueprint/01-full-design/03-irs-lite-contract-annex-20260308.md`
2. `docs/Strategy/IRS/shenwan-industry-classification.md`

Expected cleanup goal:

1. clearly separate historical SW-centric IRS wording from active generic-bucket runtime truth
2. preserve historical source value without mislabeling it as active runtime contract

---

## 3. Guardrail

Until the above cleanup happens:

1. `Phase 7 closeout` remains the active data-contract truth
2. `industry_member / l1_industry_member` remains the active runtime contract
3. `INDUSTRY_MEMBER_RUNTIME` remains the truthful IRS trace source label
