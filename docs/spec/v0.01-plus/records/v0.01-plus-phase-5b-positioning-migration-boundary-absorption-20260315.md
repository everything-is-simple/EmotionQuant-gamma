# v0.01-plus Phase 5B Positioning Migration Boundary Absorption（2026-03-15）

- **阶段**: `Phase 5B / Positioning migration boundary absorption`
- **结论**: `accepted_as_governance_only`
- **正式裁决对象**:
  - `Positioning campaign closeout`
  - `sizing lane migration boundary`
  - `partial-exit control hierarchy`
- **正式裁决结果**:
  - 主线默认运行路径保持不变
  - Positioning 可迁回主线的内容只包括 control hierarchy、条件卡规则和负面约束
  - 任何 retained / watch / residual-watch 对象都不直接升格成主线默认 sizing / exit 参数

## 1. 本轮证据

1. `positioning/03-execution/records/06-phase-p5-sizing-lane-closeout-record-20260314.md`
2. `positioning/03-execution/records/09-phase-p8-partial-exit-family-replay-record-20260315.md`
3. `positioning/03-execution/records/10-phase-p9-positioning-campaign-closeout-record-20260315.md`
4. `positioning/03-execution/records/partial-exit-lane-opening-note-20260314.md`
5. `positioning/03-execution/records/partial-exit-control-definition-note-20260314.md`
6. `positioning/03-execution/records/sizing-lane-migration-boundary-table-20260314.md`
7. `blueprint/01-full-design/09-mainline-system-operating-baseline-20260309.md`
8. `blueprint/02-implementation-spec/01-current-mainline-implementation-spec-20260308.md`
9. `blueprint/03-execution/09-phase-5b-positioning-migration-boundary-absorption-card-20260315.md`

## 2. 本轮吸收后的主线硬结论

1. `FIXED_NOTIONAL_CONTROL` 进入主线的正式身份是 `current operating control baseline`，不是默认仓位公式。
2. `SINGLE_LOT_CONTROL` 进入主线的正式身份是 `floor sanity baseline`，不是第二 operating lane。
3. `FULL_EXIT_CONTROL` 进入主线的正式身份是 `partial-exit canonical control baseline`，不是已被 retained queue 替代的旧 control。
4. `TRAIL_SCALE_OUT_25_75` 当前只能保留为 `partial-exit provisional leader`；在 Tachibana 语境下继续只允许叫 `reduce_to_core engineering proxy`。
5. `retained / residual watch / watch / no-go` 的分层边界，正式升格为主线治理术语。
6. `partial-exit lane` 不能替 `sizing lane` 擦屁股，这一条正式升格为主线 opening boundary。
7. `PX1 = locked`、`PX2 = conditional_only` 正式升格为主线条件卡规则。

## 3. 明确不迁入主线的内容

1. `WILLIAMS_FIXED_RISK / FIXED_RATIO` 的 residual-watch 身份
2. `FIXED_RISK / FIXED_VOLATILITY / FIXED_CAPITAL / FIXED_PERCENTAGE` 的 watch 身份
3. `FIXED_UNIT` 之外的任何 sizing 重新 promotion 叙事
4. `TRAIL_SCALE_OUT_33_67 / TRAIL_SCALE_OUT_50_50` 的 retained-queue 身份
5. `TRAIL_SCALE_OUT_67_33 / TRAIL_SCALE_OUT_75_25` 的 watch-queue 身份
6. 任何“已有 retained queue，所以可以自动 promotion”的口头升级

## 4. 主线 SoT 改写范围

本轮 `Phase 5B` 只允许改写以下 2 个主线正文对象：

1. `blueprint/01-full-design/09-mainline-system-operating-baseline-20260309.md`
2. `blueprint/02-implementation-spec/01-current-mainline-implementation-spec-20260308.md`

本轮不改写：

1. `Broker / Risk` 默认执行语义
2. `legacy_bof_baseline` 默认运行路径
3. 任何 sizing / exit 的运行参数默认值

## 5. 继续有效的禁止误用层

1. 不宣称“第三战场已经找到新的主线默认仓位公式”
2. 不宣称“`TRAIL_SCALE_OUT_25_75` 已经是 canonical control”
3. 不把 `SINGLE_LOT_CONTROL` 写成第二 operating lane
4. 不把 sizing residual watch 当作 partial-exit baseline
5. 不把 `PX1 / PX2` 写成自动打开的后续任务

## 6. 本轮裁决

`Phase 5B` 的正式裁决固定为：

1. `Positioning governance conclusions absorbed = yes`
2. `Positioning parameter promotion absorbed = no`
3. `control hierarchy absorbed = yes, governance_only`
4. `mainline default runtime changed = no`
5. `next_step = Phase 5C / mainline no-fake governance patch`
