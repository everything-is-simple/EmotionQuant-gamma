# v0.01-plus Phase 9D Gene Package Promotion Ruling

**Status**: `Completed`  
**Phase**: `Phase 9D / Gene package promotion ruling`  
**Date**: `2026-03-19`

---

## 1. What Phase 9D Decided

`Phase 9D` did **not** close `Phase 9` with package-level Gene runtime promotion.

Instead, it formally decided:

`defer and open a smaller follow-up package`

This means the package found meaningful isolated winners, but not enough finished evidence to truthfully claim runtime promotion yet.

---

## 2. Current Truthful Runtime

The truthful runtime boundary remains:

`legacy_bof_baseline + FIXED_NOTIONAL_CONTROL + FULL_EXIT_CONTROL + Gene sidecar only`

Therefore:

1. no Gene field is currently package-promoted into default runtime
2. `duration_percentile`, `reversal_state`, and `context_trend_direction_before` remain isolated winners only
3. `wave_role` remains `retain_sidecar_only`

---

## 3. Why Promotion Was Deferred

Promotion was deferred because the package still lacks two critical evidence layers:

1. formal replay across the frozen `Phase 9C` combination surface
2. a truthful duration-threshold sweep across `p65 / p70 / p75 / p80 / p85 / p90 / p95`

Without those layers, the package cannot yet answer:

1. whether `p95` is a real turning point or only an endpoint artifact
2. whether the duration axis stays runtime-worthy after proper sweep analysis
3. whether the three-winner frozen combinations improve the baseline enough to justify package promotion

---

## 4. Why The Package Was Not Fully Retained Either

The package was not closed as:

`retain Gene as sidecar only`

because the existing isolated results still justify narrower follow-up research:

1. `duration_percentile` remains a truthful isolated winner
2. `reversal_state` remains a truthful isolated winner
3. `context_trend_direction_before` remains a truthful isolated winner

So the truthful governance move is not to erase them.
It is to refine them.

---

## 5. Formal Ruling

`Phase 9D` is formally closed with:

1. `phase_9d_current_round_completed = yes`
2. `phase_9_package_closeout_completed = no`
3. `phase_9_package_output = defer_with_smaller_follow_up`
4. `phase_9c_formal_combination_winner = none`
5. `gene_runtime_promoted_field_count = 0`
6. `current_truthful_runtime = legacy_bof_baseline + FIXED_NOTIONAL_CONTROL + FULL_EXIT_CONTROL + Gene sidecar only`
7. `rollback_target = legacy_bof_baseline + FIXED_NOTIONAL_CONTROL + FULL_EXIT_CONTROL + Gene sidecar only`

---

## 6. Follow-Up Package Opened

The defer ruling explicitly opens:

1. `Phase 9E / duration_percentile threshold sweep`
2. `Phase 9F / frozen combination replay`

Those cards now own the missing evidence needed before any later package promotion claim may be made.

---

## 7. Next-Step Rule

After this ruling:

1. `Phase 9` remains active
2. `Phase 10` remains blocked
3. the current truthful next move is Gene refinement, not lifecycle expansion

## 8. One-Line Conclusion

`Phase 9D` did not prove that any Gene field belongs in current default runtime yet; it proved that the package must go back and finish the missing duration sweep and frozen-combination replay before package closeout can be trusted.`
