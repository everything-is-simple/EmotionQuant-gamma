# v0.01-plus Phase 6C Unified Operating Runbook Refresh (2026-03-17)

- **Phase**: `Phase 6C / unified operating runbook refresh`
- **Decision**: `completed`
- **Runbook target**: `docs/reference/operations/current-mainline-operating-runbook-20260317.md`

## 1. What Phase 6C did

`Phase 6C` formally refreshed the operating runbook so the current mainline has one truthful operating entry point after `Phase 6A` freeze and `Phase 6B` integrated validation.

The refresh writes down:

1. the real operating chain
2. the allowed and forbidden runtime switches
3. the `Gene sidecar` boundary
4. the human intervention boundary
5. rollback and emergency-stop rules

## 2. Formal runbook truth

The current truthful operating path remains:

`legacy_bof_baseline + FIXED_NOTIONAL_CONTROL + FULL_EXIT_CONTROL`

At the same time:

1. `v0.01-plus` remains the governance mainline
2. `Gene` remains `sidecar / shadow / attribution only`
3. legacy `IRS-lite / MSS-lite` runtime semantics remain retired
4. no research-line retained or watch object has been promoted into runtime default behavior

## 3. What Phase 6C explicitly forbids

1. turning `Gene` context output into a runtime hard gate
2. verbally promoting retained `Normandy` or `Positioning` objects into default runtime semantics
3. declaring unified-default cutover complete before `Phase 6 closeout`
4. rewriting default runtime behavior without a formal package and gate

## 4. Formal ruling

`Phase 6C` is formally closed with:

1. `phase_6c_runbook_refresh_completed = yes`
2. `current_mainline_runbook_refreshed = yes`
3. `default_runtime_changed = no`
4. `gene_sidecar_boundary_kept = yes`
5. `next_step = Phase 6 closeout / default-system promotion decision record`

## 5. One-line conclusion

`Phase 6C` closes the runbook gap: the unified candidate is now frozen, validated, and operationally documented, while the actual default runtime still remains unchanged until closeout.
