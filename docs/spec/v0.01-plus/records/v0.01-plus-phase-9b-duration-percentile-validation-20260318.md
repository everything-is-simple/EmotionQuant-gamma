# v0.01-plus Phase 9B Duration Percentile Validation

**Status**: `Completed`  
**Phase**: `Phase 9B / isolated duration_percentile validation`  
**Date**: `2026-03-18`

---

## 1. Position

This record does not claim that full `Gene` runtime promotion is complete.

It records one narrower fact:

`duration_percentile has now passed one truthful isolated runtime round as negative filter only.`

---

## 2. Evidence Chain

This ruling is grounded on:

1. [`../../../../blueprint/03-execution/evidence/phase-9b-duration-percentile-validation-evidence-20260318.md`](../../../../blueprint/03-execution/evidence/phase-9b-duration-percentile-validation-evidence-20260318.md)
2. [`../../../../blueprint/03-execution/records/phase-9b-duration-percentile-validation-record-20260318.md`](../../../../blueprint/03-execution/records/phase-9b-duration-percentile-validation-record-20260318.md)
3. [`../evidence/phase9b_duration_percentile_validation_legacy_duration_percentile_negative_filter_p95_w20260105_20260224_t085921__phase9_duration_validation.json`](../evidence/phase9b_duration_percentile_validation_legacy_duration_percentile_negative_filter_p95_w20260105_20260224_t085921__phase9_duration_validation.json)
4. [`../../../../blueprint/03-execution/17.1-phase-9a-gene-promoted-subset-freeze-card-20260318.md`](../../../../blueprint/03-execution/17.1-phase-9a-gene-promoted-subset-freeze-card-20260318.md)
5. [`../../../../docs/spec/v0.01-plus/records/v0.01-plus-phase-9a-gene-promoted-subset-freeze-20260318.md`](../../../../docs/spec/v0.01-plus/records/v0.01-plus-phase-9a-gene-promoted-subset-freeze-20260318.md)

---

## 3. Fixed Baseline

The baseline kept fixed in this round was:

`legacy_bof_baseline + FIXED_NOTIONAL_CONTROL + FULL_EXIT_CONTROL + Gene sidecar only`

This round changed exactly one primary variable:

`add duration_percentile >= 95 negative-filter semantics`

No other Gene field was allowed to join.

---

## 4. Formal Ruling

The formal `Phase 9B` ruling is:

`promote_duration_percentile_negative_filter`

This ruling is justified because the isolated candidate:

1. kept `signals_count` unchanged at `16`
2. blocked `6` signals, or `37.5%` of formal signals
3. blocked `6` signals that were all real `BUY filled` entries in the baseline
4. improved the full-window result versus baseline on:
   `expected_value`, `profit_factor`, `max_drawdown`, and `reject_rate`

Key full-window deltas:

1. `expected_value delta = +0.0005396858`
2. `profit_factor delta = +0.5922882381`
3. `max_drawdown delta = -0.0052306108`
4. `buy_filled_count delta = -3`

---

## 5. What Is True Now

After this record, the truthful state is:

1. `Phase 9A` remains completed
2. `Phase 9B` is now completed
3. `duration_percentile` is the first Gene runtime variable that has both:
   `formal candidate freeze + isolated runtime win`
4. its only validated runtime role is still:
   `negative filter only`

What is still not true:

1. that `Gene` has fully entered default runtime
2. that combination validation is now open
3. that `mirror / conditioning / wave_role / reversal_state / current_wave_age_band / gene_score` may join this rule

---

## 6. Residual Risk

The candidate was not perfect.

This round still exposed:

1. `missing_rate = 0.0476190476`
2. one `NO_MARKET_DATA` reject on `EXIT_300308_2026-02-11_stop_loss`

This residual does not overturn the isolated ruling, but it must remain visible.

---

## 7. Package Boundary

This record promotes `duration_percentile` only inside the `Phase 9` package progression.

It does **not** by itself declare package closeout or default runtime cutover.

Package-level promotion remains subject to:

1. later `Phase 9` package ruling
2. continued single-variable discipline
3. the existing `GX8 completed or formally non-blocking` dependency

---

## 8. One-line Conclusion

`Phase 9B` has now moved `duration_percentile` from “first legal runtime candidate” to “first isolated Gene runtime winner,” while keeping the rest of Gene outside the round.
