# v0.01-plus Phase 7B Local Data Contract Regression Audit (2026-03-18)

## 1. Scope

This audit answers one narrow question:

`Did the mainline survive the Phase 7B cutover from online-first data assumptions to the local-first runtime contract?`

Audited areas:

1. raw database freshness
2. execution-database freshness
3. active industry contract
4. local price-limit derivation
5. repository regression status

## 2. Raw Database Audit

Source database:

`G:\EmotionQuant_data\duckdb\emotionquant.duckdb`

Observed state:

1. `raw_daily = 18,221,095 rows`, max `trade_date = 20260317`
2. `raw_index_daily = 3,899,191 rows`, max `trade_date = 20260317`
3. `raw_trade_cal = 11,886 rows`, max `cal_date = 20260317`
4. `raw_stock_basic = 504,453 rows`, max `trade_date = 20260317`
5. `raw_index_classify = 10,656 rows`, max `trade_date = 20260317`
6. `raw_index_member = 300,130 rows`, max `trade_date = 20260317`

Audit ruling:

`raw local-first base = healthy`

## 3. Execution Database Audit

Execution database:

`G:\EmotionQuant_data\emotionquant.duckdb`

Observed state:

1. `l1_trade_calendar = 1,166 rows`, max `date = 2026-03-17`
2. `l1_stock_daily = 4,331,826 rows`, max `date = 2026-03-17`
3. `l1_index_daily = 779 rows`, max `date = 2026-03-17`
4. `l1_stock_info = 11,384 rows`, max `effective_from = 2026-03-17`
5. `l1_industry_member = 76,850 rows`, max `source_trade_date = 2026-03-17`

Audit ruling:

`L1 freshness = healthy`

## 4. Industry Contract Audit

Observed contract:

1. active runtime name = `industry_member / l1_industry_member`
2. old `sw_industry_member / l1_sw_industry_member` survives only as migration/history compatibility
3. active semantics = local generic industry bucket, not strict `SW2021-only`

Observed code path:

1. `src/data/fetcher.py`
2. `src/data/sw_industry.py`
3. `src/data/cleaner.py`
4. `src/selector/selector.py`

Audit ruling:

`industry contract cutover = passed`

## 5. Price-Limit Rule Audit

Observed contract:

1. `up_limit / down_limit` are derived locally on `l1_stock_daily`
2. runtime no longer depends on online `stk_limit` style downloads
3. active rule source is now local metadata + trade calendar + board semantics

Observed code path:

`src/data/fetcher.py::_apply_local_price_limits()`

Audit ruling:

`local price-limit derivation = active and truthful`

## 6. Regression Audit

Validation commands:

1. `python -m pytest -q`
2. `powershell -ExecutionPolicy Bypass -File .\scripts\ops\preflight.ps1 -Profile hook`

Observed results:

1. `pytest = 237 passed`
2. `preflight = passed`

Audit ruling:

`repository regression = passed`

## 7. Residual Risk

The cutover is stable enough to close, but three bounded risks remain:

1. local industry membership is now a truthful runtime bucket, not a full historical SW archive
2. local price-limit derivation still depends on metadata correctness for rare edge cases
3. compatibility aliases remain in migration/history layers by design

These are accepted residual boundaries, not open blockers.

## 8. Final Audit Decision

The final audit decision is:

1. `raw local-first cutover = passed`
2. `L1 contract cutover = passed`
3. `industry contract cutover = passed`
4. `price-limit rule localization = passed`
5. `repository regression audit = passed`

One-line conclusion:

`Phase 7B local data contract cutover passed its regression audit and is fit for formal closeout.`
