# v0.01-plus Phase 6B Integrated End-to-End Validation (2026-03-17)

- **Phase**: `Phase 6B / integrated end-to-end validation`
- **Decision**: `go_to_phase_6c`
- **Diagnosis**: `candidate_boundary_and_runtime_validated`
- **Validation window**: `2026-01-05 .. 2026-02-24`
- **Evidence**:
  - `docs/spec/v0.01-plus/evidence/phase6_integrated_validation_legacy_phase6_unified_default_candidate_w20260105_20260224_t181741__integrated_validation.json`

## 1. What Phase 6B validated

`Phase 6B` formally validated whether the `Phase 6A` frozen unified default-system candidate:

1. keeps the retired `IRS/MSS` runtime boundary
2. keeps `Gene` as `context sidecar / shadow-only`
3. remains replayable and interpretable across a long window and split windows
4. is credible enough to move forward to runbook consolidation

## 2. Compared scenarios

1. `RAW_LEGACY_BASELINE_CONTROL`
   - legacy runtime with legacy `IRS` and `MSS` semantics still on
   - `risk_budget` sizing
2. `PHASE6_UNIFIED_DEFAULT_CANDIDATE`
   - legacy BOF backbone with `IRS` off, `MSS` off
   - `FIXED_NOTIONAL_CONTROL`
   - `FULL_EXIT_CONTROL`
   - `Gene` as shadow-only sidecar

## 3. Formal readout

### 3.1 Raw legacy control

1. `trade_count = 7`
2. `expected_value = -0.0198605113`
3. `profit_factor = 0.2896724550`
4. `max_drawdown = 0.0114774494`
5. `exposure_rate = 0.600000`

### 3.2 Unified candidate

1. `trade_count = 13`
2. `expected_value = -0.0183010190`
3. `profit_factor = 0.6527694058`
4. `max_drawdown = 0.0242164409`
5. `exposure_rate = 0.800000`

### 3.3 Candidate minus raw legacy

1. `trade_count_ratio = 1.8571428571`
2. `buy_filled_count_ratio = 1.8571428571`
3. `expected_value_delta = +0.0015594923`
4. `profit_factor_delta = +0.3630969508`
5. `max_drawdown_delta = +0.0127389915`
6. `exposure_rate_delta = +0.2000000000`

## 4. Gate results

The formal gate checks are now:

1. `trace_complete = true`
2. `window_slice_complete = true`
3. `gene_sidecar_ready = true`
4. `retired_runtime_boundary_held = true`
5. `boundary_audit_passed = true`
6. `candidate_runtime_stable = true`

## 5. What Phase 6B did not claim

1. it did not declare `legacy_bof_baseline` already replaced as the production default runtime
2. it did not promote any research-line retained parameter into runtime default semantics
3. it did not convert `Gene` into a hard runtime gate

## 6. Formal ruling

`Phase 6B` is formally closed with:

1. `phase_6b_integrated_validation_completed = yes`
2. `phase_6b_gate_decision = go_to_phase_6c`
3. `mainline_default_runtime_changed = no`
4. `gene_runtime_hard_gate_promotion = none`
5. `next_step = Phase 6C / unified operating runbook refresh`

## 7. One-line conclusion

`Phase 6B` has now formally shown that the frozen unified candidate is boundary-clean, replayable, and stable enough to proceed to `Phase 6C`, while the default runtime still remains unchanged until closeout.
