# v0.01-plus Phase 5A Normandy Migration Boundary Absorption（2026-03-15）

- **阶段**: `Phase 5A / Normandy migration boundary absorption`
- **结论**: `accepted_as_governance_only`
- **正式裁决对象**:
  - `Normandy campaign closeout`
  - `Tachibana pilot-pack migration boundary`
  - `mainline SoT absorption delta`
- **正式裁决结果**:
  - `legacy_bof_baseline` 继续保持当前主线默认运行口径
  - Normandy 可迁回主线的内容只包括治理边界、承载方式和负面约束
  - 任何 Normandy 研究线局部 winner 都不直接升格成主线默认参数

## 1. 本轮证据

1. `normandy/03-execution/records/16-phase-normandy-campaign-closeout-record-20260313.md`
2. `normandy/03-execution/records/31-phase-n3n-tachibana-pilot-pack-migration-boundary-record-20260315.md`
3. `normandy/03-execution/records/32-phase-n3o-tachibana-pilot-pack-campaign-closeout-record-20260315.md`
4. `normandy/03-execution/evidence/tachibana_pilot_pack_migration_boundary_20260315.md`
5. `blueprint/01-full-design/09-mainline-system-operating-baseline-20260309.md`
6. `blueprint/02-implementation-spec/01-current-mainline-implementation-spec-20260308.md`
7. `blueprint/03-execution/08-phase-5a-normandy-migration-boundary-absorption-card-20260315.md`

## 2. 本轮吸收后的主线硬结论

1. `legacy_bof_baseline` 继续是当前主线默认运行路径；`BOF_CONTROL` 不迁成主线默认标签。
2. `baseline diagnosis lane` 与 `promotion lane` 的分离，正式升格为主线治理约束。
3. `N2 / N2A` 的 trailing-stop 诊断只迁成负面约束：主线当前没有证据支持全局取消或重写 trailing-stop。
4. `N1.11 / N1.12` 的质量分支结论只迁成负面约束：没有新 record 前，不得把任何 `BOF` quality branch 升格为新主位。
5. `Tachibana pilot-pack` 只允许以 `existing BOF stack + thin runner + optional hook scaffold` 的形式进入主线说明。
6. 主线当前只承认 `R4 + R5 + R6 + R7 + R10` 的 Tachibana executable boundary，不承认完整 Tachibana system ready。
7. `TRAIL_SCALE_OUT_25_75` 在主线里只能叫 `reduce_to_core engineering proxy`。

## 3. 明确不迁入主线的内容

1. `BOF_CONTROL` 作为 Normandy 研究基线的内部命名
2. `FB_BOUNDARY`、`BOF_KEYLEVEL_STRICT`、`SB_SMALL_W_MID_STRENGTH` 等 watch / observation 对象
3. `PROFIT_GATED_TRAIL_25P`、`POST_15P_TRAIL_9P` 等局部 mechanism finding
4. `CD5 / CD10` cooldown leader ordering
5. `single_lot floor sanity`
6. noncanonical side references
7. `reduced_unit_scale` executable sizing
8. `R2 / R3 / R8 / R9` 与所有仍受 `ALREADY_HOLDING` 阻塞的机制段

## 4. 主线 SoT 改写范围

本轮 `Phase 5A` 只允许改写以下 2 个主线正文对象：

1. `blueprint/01-full-design/09-mainline-system-operating-baseline-20260309.md`
2. `blueprint/02-implementation-spec/01-current-mainline-implementation-spec-20260308.md`

本轮不改写：

1. `Broker / Risk` 默认 control
2. `FIXED_NOTIONAL_CONTROL / SINGLE_LOT_CONTROL / FULL_EXIT_CONTROL` 的主线定义
3. 任何运行参数默认值

## 5. 继续有效的禁止误用层

1. 不宣称“Normandy 找到了可以直接替代主线的新默认参数”
2. 不宣称“完整 Tachibana 系统已经 ready”
3. 不把 `TRAIL_SCALE_OUT_25_75` 写成 canonical control
4. 不把 `CD10` 写成默认 cooldown
5. 不把研究线 retained / watch 对象写成主线默认项

## 6. 本轮裁决

`Phase 5A` 的正式裁决固定为：

1. `Normandy governance conclusions absorbed = yes`
2. `Normandy parameter promotion absorbed = no`
3. `Tachibana pilot boundary absorbed = yes, governance_only`
4. `mainline default runtime changed = no`
5. `next_step = Phase 5B / Positioning migration boundary absorption`
