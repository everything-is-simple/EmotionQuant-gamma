# v0.01-plus Phase 5 Research-Line Migration Package Closeout（2026-03-15）

- **阶段**: `Phase 5 / research-line migration package`
- **结论**: `Completed`
- **口径**:
  - `Phase 5` 当前完成的是：`把第二、第三战场已经收官的可迁结论压回主线 SoT、governance 与 implementation boundary`
  - `Phase 5` 当前不宣称：`研究线 retained / watch 对象已经升级为主线默认运行参数`
- **下一阶段**: `none`

## 1. 本轮证据

1. `blueprint/03-execution/07-phase-5-research-line-migration-package-card-20260315.md`
2. `blueprint/03-execution/08-phase-5a-normandy-migration-boundary-absorption-card-20260315.md`
3. `blueprint/03-execution/09-phase-5b-positioning-migration-boundary-absorption-card-20260315.md`
4. `blueprint/03-execution/10-phase-5c-mainline-no-fake-governance-patch-card-20260315.md`
5. `docs/spec/v0.01-plus/records/v0.01-plus-phase-5a-normandy-migration-boundary-absorption-20260315.md`
6. `docs/spec/v0.01-plus/records/v0.01-plus-phase-5b-positioning-migration-boundary-absorption-20260315.md`
7. `docs/spec/v0.01-plus/records/v0.01-plus-phase-5c-mainline-no-fake-governance-patch-20260315.md`
8. `blueprint/01-full-design/09-mainline-system-operating-baseline-20260309.md`
9. `blueprint/02-implementation-spec/01-current-mainline-implementation-spec-20260308.md`
10. `docs/spec/v0.01-plus/governance/v0.01-plus-mainline-no-fake-rules.md`

## 2. Phase 5 最终完成了什么

`Phase 5` 本轮正式完成了 4 件事：

1. 把第二战场可迁回主线的内容压成了 `Normandy governance boundary + Tachibana pilot boundary only`
2. 把第三战场可迁回主线的内容压成了 `control hierarchy + retained-promotion negative constraints`
3. 把主线治理层里残留的“伪默认口径”统一纠正掉
4. 把“哪些可以迁、哪些不能迁、哪些不能自动发生”写成主线正式 record

## 3. 迁回主线后现在真正成立的东西

本轮结束后，主线现在正式成立的内容固定为：

1. 当前治理主线是 `v0.01-plus`
2. 当前默认运行路径仍是 `legacy_bof_baseline`
3. `FIXED_NOTIONAL_CONTROL = current operating control baseline`
4. `SINGLE_LOT_CONTROL = floor sanity baseline`
5. `FULL_EXIT_CONTROL = partial-exit canonical control baseline`
6. `Tachibana pilot-pack` 当前只允许以 `existing BOF stack + thin runner + optional hook scaffold` 身份出现
7. `TRAIL_SCALE_OUT_25_75` 当前只允许以 `partial-exit provisional leader` / `reduce_to_core engineering proxy` 身份出现

## 4. 本轮明确没有发生的事

本轮必须同时写死，以下事情都没有发生：

1. 没有把 `DTT` 升格成当前默认运行路径
2. 没有把任何研究线 retained / watch 对象升格成主线默认参数
3. 没有把 `TRAIL_SCALE_OUT_25_75` 升格成 canonical control
4. 没有把 `CD10` 升格成默认 cooldown
5. 没有把 `SINGLE_LOT_CONTROL` 升格成第二 operating lane
6. 没有把 `Tachibana pilot-pack` 叙述成完整 Tachibana system
7. 没有自动打开 `PX1 / PX2`

## 5. 当前 still-off-limits 的对象

当前继续只能留在研究线、不能翻译成主线默认项的对象固定为：

1. `BOF_CONTROL` 作为 Normandy 内部 baseline 名称
2. `FB_BOUNDARY`、`BOF_KEYLEVEL_STRICT`、`SB_SMALL_W_MID_STRENGTH` 等 watch / observation 对象
3. `CD5 / CD10` cooldown family ordering
4. `single_lot floor sanity`
5. `WILLIAMS_FIXED_RISK / FIXED_RATIO` residual watch
6. `TRAIL_SCALE_OUT_33_67 / TRAIL_SCALE_OUT_50_50` retained queue
7. `TRAIL_SCALE_OUT_67_33 / TRAIL_SCALE_OUT_75_25` watch queue
8. `R2 / R3 / R8 / R9` 与所有仍受 `ALREADY_HOLDING` 阻塞的机制段

## 6. Phase 5 收官裁决

`Phase 5` 的正式收官裁决固定为：

1. `phase_5_defined_subcards_closed = yes`
2. `phase_5_formal_records_closed = yes`
3. `active_mainline_migration_package = none`
4. `mainline_default_runtime_changed = no`
5. `research_line_parameter_promotion = none`

## 7. 收官后的后续规则

`Phase 5` 收官后，当前如果未来还要继续，只允许两种开法：

1. `new formal mainline package`
   - 例如新的默认运行路径 promotion package
   - 或新的主线治理整改包
2. `new targeted mechanism hypothesis`
   - 必须重新回到研究线立假设、起证据链

当前明确不允许：

1. 续跑旧 `Phase 5A / 5B / 5C`
2. 把已关闭的研究线 retained / watch 对象口头升格成默认项
3. 在没有新 formal package 的前提下再次改写主线默认口径

## 8. 一句话结论

`Phase 5` 现在已经正式收官：第二、第三战场能迁回主线的边界都迁完了，该挡的假升级也已经挡住；主线因此更清楚了，但默认运行路径没有变化。`
