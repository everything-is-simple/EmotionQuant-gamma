# v0.01-plus Phase 4.1 Final Candidate Freeze（size_only_overlay，2026-03-11）

- **阶段**: `Phase 4.1 / P4.1-E`
- **结论**: `final candidate frozen`
- **治理裁决**:
  - 允许继续 `Phase 4.1`
  - 但只允许再冻结这 `1` 条最终候选
  - 当前不再允许继续扫 `carryover_buffer(2/3/...)`
- **当前最终候选**:
  - `DTT variant = v0_01_dtt_pattern_plus_irs_mss_score_size_only_overlay`
  - `MSS_MAX_POSITIONS_MODE = no_maxpos_shrink`
  - `MSS_MAX_POSITIONS_BUFFER_SLOTS = 0`

## 1. 本轮证据

1. `docs/spec/v0.01-plus/evidence/mss_remediation_candidate_freeze_dtt_v0_01_dtt_pattern_plus_irs_mss_score_size_only_overlay_w20260105_20260224_t_phase4_1_e__mss_remediation_candidate_freeze.json`
2. `docs/spec/v0.01-plus/records/v0.01-plus-phase-4-gate-replay-carryover-buffer1-20260311.md`
3. `docs/spec/v0.01-plus/records/v0.01-plus-phase-4.1-remediation-candidate-freeze-20260311.md`

## 2. 运行口径

1. 正式短窗固定为：`2026-01-05` 到 `2026-02-24`
2. 正式执行库固定为：`G:\EmotionQuant_data\emotionquant.duckdb`
3. working copy 固定落：`G:\EmotionQuant-temp\backtest`
4. evidence 固定回写：`docs/spec/v0.01-plus/evidence/`
5. 本轮继续固定三目录纪律与双 TuShare key 规则：
   - 先看 `RAW_DB_PATH / 本地旧库`
   - 再走 `TUSHARE_PRIMARY_*`
   - `TUSHARE_FALLBACK_*` 只做失败兜底

## 3. 候选语义

当前候选只改一件事：

`Broker 不再消费 max_positions shrink`

也就是：

1. `MSS` 继续产出 `risk_regime`
2. `risk_per_trade_pct / max_position_pct` 继续按 regime 缩放
3. `target_max_positions` 继续写入 trace
4. 但 `effective_max_positions` 直接回到 base `MAX_POSITIONS`

约束性结论：

`这条候选的目标不是让 MSS 消失，而是验证“问题是否主要来自 slot shrink，而不是来自 sizing overlay”。`

## 4. 场景结果

| 场景 | trade_count | EV | PF | MDD | buy_reject_maxpos_count | participation_rate |
|---|---:|---:|---:|---:|---:|---:|
| `baseline_no_overlay` | `16` | `-0.003532` | `0.4968` | `0.017079` | `0` | `1.0000` |
| `current_full_overlay_hard_cap` | `9` | `-0.008988` | `0.4613` | `0.006035` | `7` | `0.5625` |
| `candidate_full_overlay_size_only` | `16` | `-0.003532` | `0.4968` | `0.009241` | `0` | `1.0000` |

## 5. 关键比较

### 5.1 相对当前 `hard_cap`

1. `trade_set_changed_count = 7`
2. `quantity_changed_count = 0`
3. `reject_set_changed_count = 7`
4. `candidate_only_trade_count = 7`
5. `candidate_only_entry_pnl_total = +800.3125`
6. `relieved_maxpos_reject_count = 7`

约束性结论：

`size_only_overlay` 已经把 `hard_cap` 路径里的 7 笔 `MAX_POSITIONS_REACHED` 拒单全部释放回来，而且这一步没有再引入新的 quantity 路径差异。`

### 5.2 相对 `baseline_no_overlay`

1. `trade_set_changed_count = 0`
2. `quantity_changed_count = 14`
3. `reject_set_changed_count = 0`
4. `buy_reject_maxpos_count = 0`

约束性结论：

`当前候选和 baseline_no_overlay 的差异，已经退化成纯 sizing-only overlay。`

这意味着：

1. `slot shrink` 已经被从 Broker 执行面剥离干净
2. 当前若还存在收益差异，主因就不再是 `max_positions` 拒单链
3. 下一轮若仍然打不过 `legacy`，就不能继续把问题归因成“slot 语义没调对”

## 6. 当前裁决

1. `size_only_overlay` 是 `Phase 4.1` 允许进入最终 replay 的最后一条候选
2. 它相对 `current_full_overlay_hard_cap` 已同时改善：
   - `EV`
   - `PF`
   - `MDD`
   - `buy_reject_maxpos_count`
3. 因此它具备进入最后一次 direct `Phase 4 Gate replay` 的资格

## 7. 下一步

当前下一步固定为：

`legacy_bof_baseline` vs `v0_01_dtt_pattern_plus_irs_mss_score_size_only_overlay`

并继续用同一套 direct Gate bundle：

1. `matrix replay`
2. `idempotency`
3. `trade attribution`
4. `rank decomposition`
5. `windowed sensitivity`

如果这次 direct replay 仍然 `NO-GO`，则：

1. 正式结束 `Phase 4.1`
2. 不再继续冻结新的 `MSS / Broker` 候选
3. 默认路径继续保持 `legacy_bof_baseline`
