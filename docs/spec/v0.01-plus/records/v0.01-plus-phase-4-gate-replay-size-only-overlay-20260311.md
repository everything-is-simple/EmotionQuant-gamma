# v0.01-plus Phase 4 Gate Replay（size_only_overlay）记录（2026-03-11）

- **阶段**: `Phase 4 / Gate replay`
- **结论**: `NO-GO persists`
- **附加治理结论**:
  - `Phase 4.1 / MSS-Broker Remediation` 已完成并关闭
  - `size_only_overlay` 是 `Phase 4.1` 允许进入 direct Gate replay 的最后一条候选
  - 当前默认路径继续保持 `legacy_bof_baseline`
- **正式裁决对象**:
  - `legacy_bof_baseline`
  - `v0_01_dtt_pattern_plus_irs_mss_score_size_only_overlay`

## 1. 本轮证据

1. `docs/spec/v0.01-plus/evidence/matrix_summary_dtt_phase4_gate_replay_legacy_vs_size_only_overlay_w20260105_20260224_t_phase4_replay__dtt_matrix.json`
2. `docs/spec/v0.01-plus/evidence/idempotency_legacy_legacy_bof_baseline_w20260105_20260224_t_phase4_gate_replay_size_only__idempotency_check.json`
3. `docs/spec/v0.01-plus/evidence/idempotency_dtt_v0_01_dtt_pattern_plus_irs_mss_score_size_only_overlay_w20260105_20260224_t_phase4_gate_replay_size_only__idempotency_check.json`
4. `docs/spec/v0.01-plus/evidence/trade_attribution_legacy_bof_baseline_vs_v0_01_dtt_pattern_plus_irs_mss_score_size_only_overlay_w20260105_20260224_t_phase4_gate_replay_size_only__trade_attribution.json`
5. `docs/spec/v0.01-plus/evidence/rank_decomposition_legacy_bof_baseline_to_v0_01_dtt_pattern_plus_irs_mss_score_size_only_overlay_w20260105_20260224_t_phase4_gate_replay_size_only__rank_decomposition.json`
6. `docs/spec/v0.01-plus/evidence/windowed_sensitivity_legacy_bof_baseline_vs_v0_01_dtt_pattern_plus_irs_mss_score_size_only_overlay_w20251222_20260224_t_phase4_gate_replay_size_only__windowed_sensitivity.json`
7. `docs/spec/v0.01-plus/records/v0.01-plus-phase-4.1-size-only-candidate-freeze-20260311.md`

## 2. 运行口径

1. 正式短窗 Gate replay 固定为：`2026-01-05` 至 `2026-02-24`
2. 更长窗口 sensitivity 固定为：`2025-12-22` 至 `2026-02-24`
3. 比较对象固定为：
   - `legacy_bof_baseline`
   - `v0_01_dtt_pattern_plus_irs_mss_score_size_only_overlay`
4. 正式源库固定为：`G:\EmotionQuant_data\emotionquant.duckdb`
5. working copy / artifact cache 固定落：`G:\EmotionQuant-temp`
6. evidence 固定回写：`docs/spec/v0.01-plus/evidence/`
7. 本轮继续固定三目录纪律与双 TuShare key 规则：
   - 先看 `RAW_DB_PATH / 本地旧库`
   - 再走 `TUSHARE_PRIMARY_*`
   - `TUSHARE_FALLBACK_*` 只做失败兜底

## 3. Gate Replay 结果

### 3.1 Matrix Replay

| 场景 | trade_count | EV | PF | MDD | reject_rate | participation_rate |
|---|---:|---:|---:|---:|---:|---:|
| `legacy_bof_baseline` | `5` | `0.010740` | `2.2327` | `0.003867` | `0.0000` | `1.0000` |
| `v0_01_dtt_pattern_plus_irs_mss_score_size_only_overlay` | `16` | `-0.003532` | `0.4968` | `0.009241` | `0.0000` | `1.0000` |

相对 `legacy_bof_baseline`：

1. `expected_value` 更低：`-0.003532 < 0.010740`
2. `profit_factor` 更低：`0.4968 < 0.95 * 2.2327`
3. `max_drawdown` 更高：`0.009241 > 1.10 * 0.003867`
4. `skip_maxpos_count` 已归零，但 formal Gate 数值没有翻转

约束性结论：

`size_only_overlay` 已把 slot shrink 从 formal replay 中剥离掉，但 short-window direct Gate replay 仍然直接命中 NO-GO 阈值。`

### 3.2 Idempotency

本轮直接把两侧正式比较对象都补了幂等：

1. `legacy_bof_baseline`
   - `metrics_equal = true`
   - `trade_count = 5`
   - `expected_value = 0.010740`
   - `profit_factor = 2.232724`
   - `max_drawdown = 0.003867`
2. `size_only_overlay`
   - `metrics_equal = true`
   - `trade_count = 16`
   - `expected_value = -0.003532`
   - `profit_factor = 0.496817`
   - `max_drawdown = 0.009241`

约束性结论：

`这次 replay 的差距不是幂等噪音，而是稳定可重放的对象差异。`

### 3.3 Direct Trade Attribution（formal `top50_pos10`）

formal default 场景固定看：`top50_pos10`。

聚合后：

1. `changed_days = 8`
2. `legacy_changed_entry_pnl_total = +5239.4632`
3. `candidate_changed_entry_pnl_total = -3803.9520`
4. `net_delta(candidate - legacy) = -9043.4152`

最关键的变化日：

1. `2026-01-12`
   - legacy 独有 `002475`
   - `entry_pnl = -3888.9608`
   - 候选在这一天避免了这笔亏损
2. `2026-02-02`
   - 候选独有 `300014`
   - `entry_pnl = -2664.8278`
3. `2026-02-05`
   - 候选独有 `300308 = -3788.1203`
   - 同时 `002175 / 002547` 出现数量路径改写
   - 当日 `net_delta = -4822.1166`
4. `2026-02-06`
   - 两侧都成交 `002384`
   - 但 legacy `1300` 股，候选只有 `500` 股
   - `entry_pnl: +10326.1210 -> +3971.5850`
   - 当日 `net_delta = -6354.5360`

约束性结论：

`size_only_overlay` 并不是“全程都错”，但 formal default 场景下最关键的收益缺口已经不再来自 MAX_POSITIONS 拒单，而是新增交易与数量路径共同把结果拉坏。`

### 3.4 Direct Rank / Execution Decomposition

短窗 direct rank decomposition 的核心事实是：

1. `legacy_bof_baseline` 没有 `DTT rank sidecar`
2. `shared_signal_count = 0`
3. `right_only_count = 16`
4. `buy_trade_impact.trade_set_changed_count = 15`
5. `buy_trade_impact.quantity_changed_count = 3`
6. `maxpos_reject_impact.reject_set_changed_count = 0`

约束性结论：

`size_only_overlay` 已经把 MAX_POSITIONS 拒单差异清零，但 direct replay 仍然因为执行集合与数量路径改写而落后于 legacy。当前问题主因不再是 slot shrink，而是 sizing-only overlay 和 direct legacy 路径整体仍不匹配。`

### 3.5 Windowed Sensitivity

`completed_count = 5`，`failed_count = 0`。

共同结论：

1. 更长窗口下结果仍然是 mixed，而不是单边压倒性胜出
2. tighter capacity 场景下，`candidate` 会优于 `legacy`
3. 但更接近 formal default 的 `top50_pos10`，仍然不能稳定推翻 `legacy`

代表性结果：

1. `top1_pos1`
   - `legacy EV = -0.037475`
   - `candidate EV = 0.023198`
   - `candidate PF / MDD` 都更好
2. `top1_pos2`
   - `legacy EV = -0.031602`
   - `candidate EV = 0.014443`
   - `candidate PF / MDD` 都更好
3. `top50_pos10`
   - `legacy EV = -0.016841`
   - `candidate EV = -0.006605`
   - `legacy PF = 0.9142`
   - `candidate PF = 0.4955`
   - `legacy MDD = 0.015721`
   - `candidate MDD = 0.011680`

约束性结论：

`更长窗口 direct replay 呈现 mixed outcome：candidate 在 EV/MDD 上有时更好，但 PF 并不稳定占优。这不足以推翻 short-window formal Gate replay 对 legacy 的 NO-GO 结论。`

## 4. 正式裁决

按 `blueprint/03-execution/06-phase-4-gate-card-20260309.md` 的冻结标准，本轮继续命中 `NO-GO`：

1. `Matrix Replay` 已完成
2. `legacy / candidate` 双边 `idempotency` 已通过
3. direct attribution bundle 已补齐
4. 但相对 `legacy_bof_baseline`：
   - `expected_value < legacy`
   - `profit_factor < 0.95 * legacy`
   - `max_drawdown > 1.10 * legacy`

因此本轮正式裁决为：

`Phase 4 Gate replay (size_only_overlay) = NO-GO persists`

## 5. Phase 4.1 Closing Decision

1. `P4.1-F` 已完成
2. `Phase 4.1` 卡内约束已触发：
   - 这是允许执行的最后一个任务
   - replay 仍然 `NO-GO`
3. 因此 `Phase 4.1 / MSS-Broker Remediation` 正式关闭
4. 当前不再继续冻结新的 `MSS / Broker` 候选

## 6. Rollback Condition（维持不变）

1. 正式回退目标继续保持：
   - `legacy_bof_baseline`
2. 当前默认路径不切到：
   - `v0_01_dtt_pattern_plus_irs_mss_score_size_only_overlay`
3. `Phase 4.1` 产生的证据保留，但不升级为正式默认

## 7. 本轮结论

1. `size_only_overlay` 已完成 final direct Gate replay
2. 它证明了：`max_positions shrink` 的确是前一轮主因，但把它拿掉仍不足以打过 `legacy`
3. formal default 场景下，当前失败主因已从 `MAX_POSITIONS` 拒单链转成 `trade_set + quantity` 路径差异
4. `Phase 4.1` 到此正式关闭
5. 当前正式默认路径保持不变：
   - `legacy_bof_baseline`
