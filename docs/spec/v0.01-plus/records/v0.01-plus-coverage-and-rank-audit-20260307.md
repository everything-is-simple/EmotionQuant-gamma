# v0.01-plus 数据覆盖与排序拆解记录（2026-03-07）

- **范围**: `2026-01-05` 至 `2026-02-24`
- **目标**: 查清 `2026-02-10 / 02-11 / 02-13` 的覆盖异常修复情况，并验证 `IRS` 排序差异是否已经进入执行约束
- **执行库**: `G:\EmotionQuant_data\emotionquant.duckdb`
- **raw 源库**: `G:\EmotionQuant_data\duckdb\emotionquant.duckdb`
- **工作副本**: `G:\EmotionQuant-temp\backtest\dtt-matrix-20260307.duckdb`, `G:\EmotionQuant-temp\backtest\rank_decomp_*.duckdb`
- **证据**:
  - `docs/spec/v0.01-plus/evidence/coverage_audit_dtt_v0_01_dtt_bof_plus_irs_score_w20260210_20260213_t163939__coverage_audit.json`
  - `docs/spec/v0.01-plus/evidence/execution_sensitivity_dtt_v0_01_dtt_bof_only_vs_v0_01_dtt_bof_plus_irs_score_w20260105_20260224_t162521__execution_sensitivity.json`

## 1. 数据覆盖修复审计

| 日期 | execution `l1_stock_daily` | execution `l2_stock_adj_daily` | execution `l3_irs_daily` | raw `raw_daily` | 结论 |
|---|---:|---:|---:|---:|---|
| `2026-02-09` | 5465 | 5465 | 31 | 5465 | 正常 |
| `2026-02-10` | 5470 | 5470 | 31 | 5470 | 已修复 |
| `2026-02-11` | 5470 | 5470 | 31 | 5470 | 已修复 |
| `2026-02-13` | 5474 | 5474 | 31 | 5474 | 已修复 |
| `2026-02-24` | 5474 | 5474 | 31 | 5474 | 正常 |

修复后的焦点日期市场分布恢复为完整截面：`主板 / 创业板 / 科创板 / 北交所` 四类均存在。

### 直接判断

1. `2026-02-10 / 02-11 / 02-13` 的 raw 与 execution 已重新对齐，`31` 行截断已经消失。
2. `l3_irs_daily=31` 对应的是行业截面行数，不再是“IRS 没算出来”；当前焦点已经从数据断层转到执行约束解释。
3. 因此，之前关于 “raw 截断导致短窗结果失真” 的疑点已经被排除。

## 2. 宽约束下的排序与执行拆解（`top50_pos10`）

| 指标 | 数值 |
|---|---:|
| 共享信号数 | 16 |
| 发生名次变化 | 10 |
| 发生入选集合变化 | 0 |
| 发生实际成交集合变化 | 0 |
| 发生实际买入数量变化 | 9 |
| 多信号日期 | 4 |
| 单日最大共享信号数 | 3 |
| 发生数量变化的执行日 | `2026-01-20`、`2026-01-30`、`2026-02-04`、`2026-02-05` |

直接判断：

1. `IRS` 在宽约束下仍然只改变 `4` 个多信号日的名次：`2026-01-19`、`2026-01-29`、`2026-02-03`、`2026-02-04`。
2. `Strategy` 的 `Top-N` 入选集合没有变化，但 `Broker` 的现金预占顺序已经被改变，最终导致同日多个信号的买入数量发生变化。
3. 这说明“`IRS` 没有进入执行约束”的旧结论已经不成立；更准确的说法是：在宽约束下它不改成交集合，但会改实际仓位数量。

样例：

- `2026-01-20`
  - `600010`: `38100 -> 42300`
  - `600340`: `58300 -> 52500`
- `2026-02-04`
  - `002227`: `9800 -> 8800`
  - `002517`: `3800 -> 4300`

## 3. 收紧约束后的敏感性矩阵

| 场景 | rank_changed | selected_changed | trade_set_changed | reject_set_changed | 结论 |
|---|---:|---:|---:|---:|---|
| `top1_pos1` | 10 | 8 | 0 | 8 | `IRS` 已进入 Broker `MAX_POSITIONS` 风控边界 |
| `top1_pos2` | 10 | 8 | 4 | 4 | `IRS` 已改变实际 `BUY` 成交集合 |
| `top2_pos1` | 10 | 4 | 0 | 4 | `IRS` 已进入 Broker `MAX_POSITIONS` 风控边界 |
| `top2_pos2` | 10 | 4 | 4 | 8 | `IRS` 已改变实际 `BUY` 成交集合 |
| `top50_pos10` | 10 | 0 | 0 | 0 | `IRS` 已改变实际买入数量 |

关键执行日：

- `2026-01-20`
  - `top1_pos2 / top2_pos2` 下，`600340` 与 `600010` 的成交身份发生互换
- `2026-02-04`
  - `top1_pos2 / top2_pos2` 下，`002227` 与 `002517` 的成交身份发生互换
- `2026-01-30`、`2026-02-05`
  - `top1_pos1 / top2_pos1` 下，`MAX_POSITIONS` 拒单对象发生互换

## 4. 综合判断

1. raw 源库与执行库的异常覆盖已经修复，`31` 行截断不再是当前阻塞。
2. `IRS` 不仅进入了 `DTT` 排序层，而且已经在多个场景下进入了 `Strategy Top-N`、`Broker MAX_POSITIONS` 与实际 `BUY` 成交集合。
3. 宽约束下 `IRS` 的主要影响体现在资金预占顺序和买入数量，而不是成交身份切换。
4. `MSS` 目前仍缺乏能够解释实际执行差异的独立证据，在短窗里暂时只表现为分数平移项。

## 5. 下一步建议

1. 把 `2026-01-20 / 01-30 / 02-04 / 02-05` 这 4 个执行日单独做交易归因，判断数量变化与成交身份互换是否改善盈亏。
2. 把敏感性矩阵扩到更长窗口，确认 `IRS` 对执行约束的影响不是短窗偶然现象。
3. `MSS` 继续保留为候选变体，但在出现“名次变化或成交变化”之前，不应宣称其已经对排序有实质贡献。
