# v0.01-plus Phase 6A Promoted Subset Freeze（2026-03-17）

- **阶段**: `Phase 6A / promoted subset freeze`
- **结论**: `freeze_applied`
- **正式裁决对象**:
  - `unified default system promotion candidate`
  - `promote-now / shadow-only / remain-research matrix`
  - `Gene context sidecar boundary`
- **正式裁决结果**:
  - 当前统一默认系统候选已冻结成合法组成边界
  - 当前允许进入 promotion candidate 的 runtime 核心仍只有 `BOF baseline + control hierarchy`
  - 第四战场当前只以 `context sidecar / dashboard / attribution` 身份进入主线候选，不进入 runtime hard gate

## 1. 本轮证据

1. `blueprint/03-execution/11-phase-6-unified-default-system-migration-package-card-20260317.md`
2. `blueprint/03-execution/12-phase-6a-promoted-subset-freeze-card-20260317.md`
3. `docs/spec/v0.01-plus/governance/v0.01-plus-mainline-no-fake-rules.md`
4. `docs/spec/v0.01-plus/governance/v0.01-plus-promoted-subset-freeze-20260317.md`
5. `docs/spec/v0.01-plus/records/v0.01-plus-phase-5-research-line-migration-package-closeout-20260315.md`
6. `normandy/03-execution/records/16-phase-normandy-campaign-closeout-record-20260313.md`
7. `positioning/03-execution/records/10-phase-p9-positioning-campaign-closeout-record-20260315.md`
8. `gene/03-execution/records/11-phase-g8-gene-campaign-closeout-record-20260316.md`
9. `docs/spec/common/records/four-battlefields-integrated-system-map-20260316.md`

## 2. 本轮 freeze 后的主线硬结论

1. 当前统一默认系统候选的 runtime backbone 继续固定为 `legacy_bof_baseline`。
2. `FIXED_NOTIONAL_CONTROL` 继续作为 `current operating control baseline` 进入统一默认系统候选。
3. `SINGLE_LOT_CONTROL` 继续只保留为 `floor sanity baseline`，不进入第二 operating lane。
4. `FULL_EXIT_CONTROL` 继续作为 `partial-exit canonical control baseline` 进入统一默认系统候选。
5. `Gene` 当前允许进入主线候选的内容，只包括 `stock self-history tags / mirror ranks / conditioning readout` 的 `context sidecar / dashboard / attribution` 身份。
6. 旧 `IRS-lite / MSS-lite` 继续保持退役状态，不恢复为默认 runtime layer。

## 3. 明确不进入当前 promotion candidate 的内容

1. 任何旧 `IRS / MSS` 默认运行回归
2. 任何 `Normandy` watch / retained branch 的自动 promotion
3. 任何 `Positioning` retained partial-exit family 的默认升格
4. 任何 `Gene` 对 runtime 的硬 gate、硬 filter、默认排序翻译
5. `Tachibana pilot-pack` 作为完整默认系统的替身叙述

## 4. 当前统一默认系统候选的正式分层

### 4.1 Promote-Now

1. `legacy_bof_baseline`
2. `FIXED_NOTIONAL_CONTROL`
3. `SINGLE_LOT_CONTROL (governance only)`
4. `FULL_EXIT_CONTROL`

### 4.2 Shadow-Only

1. `stock self-history tags`
2. `market / industry mirror ranks`
3. `gene conditioning readout`

### 4.3 Remain-Research

1. `Normandy watch / retained branches`
2. `TRAIL_SCALE_OUT_25_75`
3. `TRAIL_SCALE_OUT_33_67 / 50_50 / 67_33 / 75_25`
4. `Tachibana pilot-pack`

## 5. 本轮裁决

`Phase 6A` 的正式裁决固定为：

1. `promotion_candidate_freeze_applied = yes`
2. `gene_context_boundary_absorbed = yes, shadow_only`
3. `research_line_parameter_promotion = none`
4. `mainline_default_runtime_changed = no`
5. `next_step = Phase 6B / integrated end-to-end validation`

## 6. 一句话结论

`Phase 6A` 已把统一默认系统候选的合法组成边界写死：当前能进默认系统候选的 runtime 核心仍是 `BOF baseline + control hierarchy`，第四战场只以 context sidecar 身份进入，不得偷变成 runtime hard gate。
