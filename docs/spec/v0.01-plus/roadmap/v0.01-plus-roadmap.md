# v0.01-plus 路线图（当前主开发线）

- **执行基线**: `docs/design-v2/01-system/system-baseline.md`（`v0.01 Frozen` 历史基线，用于对照与回退参考）
- **当前设计入口**: `docs/design-v2/03-algorithms/core-algorithms/down-to-top-integration.md`
- **状态来源**: `docs/spec/common/records/development-status.md`
- **文档状态**: Active（当前主开发线；用于替代 legacy top-down）
- **冲突规则**: 涉及 `v0.01` 历史口径时，以 `system-baseline.md` 为准；涉及 `v0.01-plus` 当前主开发线时，以本文件和对应实现卡为准。

## 1. 目标

`v0.01-plus` 的目标不再是“独立实验版验证”，而是作为 `v0.01` legacy top-down 的主线替代版本，完成 `down-to-top / DTT` 链路的实现、证据收口与默认运行路径切换。

本版本的核心使命：

1. 让 `Selector` 退回基础过滤与规模控制。
2. 让 `Strategy` 承担 `BOF -> 后置评分 -> 排序` 的主职责。
3. 让 `legacy_bof_baseline` 降级为对照组与回退路径，而不是继续充当默认主链。

## 2. 版本边界

1. `v0.01 Frozen` 保持不动：`BOF` 单形态、`T+1 Open`、历史正式结果契约字段冻结。
2. `v0.01-plus` 是当前主开发线，默认目标是 `DTT` 主链，而不是继续维护 legacy top-down。
3. `legacy top-down` 仅保留为 compare / rollback 模式，不再作为当前主开发线的默认交付目标。
4. 当前阶段采用“兼容优先”的迁移策略：下游 `Broker / Backtest / Report` 先保持消费兼容，`DTT` 排序与明细通过 `l3_signal_rank_exp` 承载；是否升级正式 `Signal` 契约，后续单独决策。
5. `v0.02` 不承接本次链路替代；当前主线替代工作全部留在 `v0.01-plus` 内完成。

## 3. 主线切换验证矩阵

| variant | Selector | Strategy | 用途 |
|---|---|---|---|
| `legacy_bof_baseline` | `基础过滤 + MSS/IRS 漏斗` | `BOF 单形态触发` | 历史对照组 / 回退参考 |
| `v0_01_dtt_bof_only` | `基础过滤后直接进 BOF` | 仅用 `bof_strength` 排序 | 验证纯后置排序基线 |
| `v0_01_dtt_bof_plus_irs_score` | `基础过滤后直接进 BOF` | `bof_strength + irs_score` | 当前优先候选主链 |
| `v0_01_dtt_bof_plus_irs_mss_score` | `基础过滤后直接进 BOF` | `bof_strength + irs_score + mss_score` | 验证市场/行业双后置加权 |

## 4. 交付范围

1. `roadmap/v0.01-plus-spec-01-selector-strategy.md`：主线实现卡与模块边界。
2. `governance/v0.01-plus-gate-checklist.md`：主线切换 Gate 与回退门。
3. `governance/v0.01-plus-data-contract-table.md`：兼容优先迁移期的数据契约补充。
4. `governance/v0.01-plus-run-artifact-rules.md`：`run_id / variant / artifact` 固定规则。
5. 主线证据统一落在 `docs/spec/v0.01-plus/evidence/`。
6. 主线记录统一落在 `docs/spec/v0.01-plus/records/`。

## 5. Go / No-Go

只有在以下条件同时满足时，才允许把 `v0.01-plus` 切成默认运行路径：

1. 四组对照均能在同一数据快照、同一参数快照、同一成本模型下重跑。
2. 新主链相对 `legacy_bof_baseline` 的改进不是靠样本显著收缩或过滤路径失真换来的假象。
3. `legacy` 回退路径仍然可用，且回退成本清楚可控。
4. `docs/observatory/sandbox-review-standard.md` 的七维审查结论可支撑切换。

若上述条件不满足，则结论为：`v0.01-plus` 继续留在当前主开发线内修正，但默认运行路径暂不切换；不得把这次链路替代工作改名塞进 `v0.02`。

## 6. 当前进展（2026-03-08）

已完成：

1. 固定四组矩阵已跑通：
   - `legacy_bof_baseline`
   - `v0_01_dtt_bof_only`
   - `v0_01_dtt_bof_plus_irs_score`
   - `v0_01_dtt_bof_plus_irs_mss_score`
2. 默认主线 `v0_01_dtt_bof_plus_irs_score` 的幂等验证已通过。
3. 每组 run 均已写入 `config_hash / data_snapshot / git_commit / variant`。
4. 四个焦点执行日的逐笔归因已完成，已经能解释 `IRS` 改了哪些票、哪些数量、哪些天变好、哪些天变差。
5. 长窗口敏感性已改成“分窗口 + 分场景”执行，并新增批处理脚本 `scripts/backtest/run_v001_plus_windowed_sensitivity.py`。
6. `BOF` 执行路径已做第一轮内存优化：批量预取历史、分批计算、DTT 临时表落地、最后统一排名。

当前结论：

1. `legacy` 与 `DTT` 已切开，`DTT` 在该短窗内的交易数明显高于 `legacy`。
2. `2026-02-10 / 02-11 / 02-13` 的 raw 与 execution 覆盖已修复，`31` 行截断不再存在。
3. 排序拆解与敏感性矩阵共同表明：`IRS` 已在 `4` 个多信号交易日改变 `final_rank`，并且已经进入 `Top-N / MAX_POSITIONS / BUY 数量` 约束。
4. 在 `top1_pos2 / top2_pos2` 场景中，`IRS` 会直接改变实际成交集合；在 `top50_pos10` 宽场景中，`IRS` 仍会改变实际买入数量。
5. `MSS` 当前仍只体现为分数平移项，没有独立证据证明其改变了横截面排序或执行结果。
6. 长窗口证据进一步表明：`IRS` 已具备“影响执行”的能力，但收益改善并不稳定。
7. 当前推荐结论为 `NO-GO`：`v0.01-plus` 继续作为主开发线推进，但默认运行路径暂不切到 `DTT`。
8. 第一轮 `BOF` 内存优化后，更长窗口五个敏感性场景已全部在 `4GB` 会话内存限制下跑通。
9. `EG5` 仍未完成，因为七维评审 `S0/S1=0` 尚未正式收口；当前阻塞点已经从 OOM 转成收益稳定性与回退条件解释。

当前证据：

1. `docs/spec/v0.01-plus/evidence/matrix_summary_dtt_v0_01_dtt_bof_plus_irs_score_w20260105_20260224_t160708__dtt_matrix.json`
2. `docs/spec/v0.01-plus/evidence/idempotency_dtt_v0_01_dtt_bof_plus_irs_score_w20260105_20260224_t151843__idempotency_check.json`
3. `docs/spec/v0.01-plus/evidence/coverage_audit_dtt_v0_01_dtt_bof_plus_irs_score_w20260210_20260213_t163939__coverage_audit.json`
4. `docs/spec/v0.01-plus/evidence/execution_sensitivity_dtt_v0_01_dtt_bof_only_vs_v0_01_dtt_bof_plus_irs_score_w20260105_20260224_t162521__execution_sensitivity.json`
5. `docs/spec/v0.01-plus/records/v0.01-plus-short-window-matrix-20260307.md`
6. `docs/spec/v0.01-plus/records/v0.01-plus-coverage-and-rank-audit-20260307.md`
7. `docs/spec/v0.01-plus/evidence/trade_attribution_dtt_v0_01_dtt_bof_only_vs_v0_01_dtt_bof_plus_irs_score_w20260105_20260224_t165536__trade_attribution.json`
8. `docs/spec/v0.01-plus/evidence/windowed_sensitivity_dtt_v0_01_dtt_bof_only_vs_v0_01_dtt_bof_plus_irs_score_w20251222_20260224_t_after_opt__windowed_sensitivity.json`
9. `docs/spec/v0.01-plus/records/v0.01-plus-trade-attribution-and-windowed-sensitivity-20260308.md`
