# v0.01 Week2 Selector/Strategy Evidence

日期：`2026-03-06`

## 范围

本证据包覆盖 Week2 的两条主链：

1. `spec-02` 三组漏斗消融：`BOF baseline -> BOF+MSS -> BOF+MSS+IRS`
2. `spec-03` 一次真实 `select_candidates -> generate_signals` smoke 联调

所有运行都基于执行库 `G:\EmotionQuant_data\emotionquant.duckdb` 的 working copy，避免锁冲突和误改运行时表。

## Smoke 结果

- 运行日期：`2026-01-09`
- 证据文件：`docs/spec/v0.01/evidence/v0.01-selector-strategy-smoke-20260306-v2.json`
- `build_rows = 112`
- `candidates_count = 100`
- `signals_count = 1`
- 样例信号：`002475_2026-01-09_bof`

结论：`Selector -> Strategy` 主链已可执行，且不是空跑；在真实执行库快照上能产出一条 BOF BUY 信号。

## 短窗消融结果

- 窗口：`2026-01-05` 至 `2026-02-24`
- 证据文件：`docs/spec/v0.01/evidence/v0.01-selector-ablation-short-20260306.json`

结果摘要：

| 场景 | trade_count | win_rate | EV | PF | MDD | participation |
|---|---:|---:|---:|---:|---:|---:|
| `bof_baseline` | 12 | 0.416667 | -0.010833 | 0.780863 | 0.021103 | 1.000000 |
| `bof_plus_mss` | 12 | 0.416667 | -0.010833 | 0.780863 | 0.021103 | 1.000000 |
| `bof_plus_mss_plus_irs` | 0 | 0.000000 | 0.000000 | 0.000000 | 0.000000 | 0.000000 |

结论：

1. 在该短窗内，`MSS gate` 没有改变 BOF baseline 的交易结果。
2. 在重刷 L3 后，`IRS filter` 在该窗口内把机会直接压到 `0`，说明当前行业过滤配置过强，尚不能纳入 v0.01 默认基线。
3. `environment_breakdown` 全部落在 `NEUTRAL`，与 MSS 审计结论一致：当前 MSS 尚未形成有效环境切分。

## 分布审计结果

- 窗口：`2023-01-03` 至 `2026-02-24`
- 初轮证据文件：已清理；对应初轮结论保留在本页与 `docs/spec/v0.01/records/cleanup-candidates-20260306.md`
- 当前权威证据文件：`docs/spec/v0.01/evidence/v0.01-selector-distribution-audit-20260306-v2.json`

关键发现：

1. 本节记录的是 Week2 初轮分布审计结论，不再作为当前权威口径。
2. 初轮审计曾暴露 `MSS 全 NEUTRAL`、`IRS=111` 行业桶、以及全周期消融性能阻塞三项风险。
3. 这些结论已在同日后续工作中被重算和收口：行业链路已回到正式 `SW31`，当前权威 IRS 审计见 `v0.01-selector-distribution-audit-20260306-v2.json`。
4. MSS 也已完成 baseline calibration，当前权威校准结果见 `docs/spec/v0.01/evidence/v0.01-mss-baseline-calibration-20260306.json`。

当前解释：

- 这份初轮审计的价值在于暴露问题，而不是定义当前口径。
- 当前正式口径已经更新为：`SW31 + 校准后 MSS baseline`。

## 全周期阻塞

2026-03-06 尝试执行全周期三场消融：

`python scripts/backtest/run_week2_ablation.py --start 2023-01-03 --end 2026-02-24 --patterns bof --skip-rebuild-l3 ...`

结果：

1. 在已复用 L3 的前提下，进程运行超过 `75` 分钟仍未收口。
2. 本轮已中止该进程，避免长期占用 working copy。
3. 结论不是“无结果”，而是“当前引擎性能不足以支撑 Week2 全周期三场消融”。

## 当前解释

这批证据回答的是“Week2 主链是否已经能跑、能对照、能留痕”。

当前答案是：能。

但它还不是 Week2 的最终封单证据。要正式封 Week2，仍需补：

1. 全周期三场消融的可执行性能问题
2. `gate mode` 是否继续维持当前 `BEARISH-only` 语义，还是引入更强的 MSS 过滤语义
3. IRS Top-N 与 MSS/IRS 联动覆盖的正式消融
