# v0.01 BOF Baseline 证据包（第2版）

**日期**: 2026-03-05  
**执行人**: Codex  
**数据库**: `G:\EmotionQuant_data\emotionquant.duckdb`  
**数据来源**: 本地历史库 `G:\EmotionQuant_data\duckdb\emotionquant.duckdb`（raw_* 表映射）

## 1. 本轮目标

1. 不再重复下载三年数据。
2. 直接复用本地 2000-2026 历史库，填充当前执行库 `l1_*`。
3. 跑通 `build -> backtest`，生成可交易样本证据。

## 2. 数据可用性检查（本地库）

源库关键表覆盖：

- `raw_daily`: `20000104 -> 20260224`
- `raw_daily_basic`: `20000104 -> 20260224`
- `raw_trade_cal`: `20000101 -> 20260225`
- `raw_stock_basic`: `20100101 -> 20260201`
- `raw_index_daily`: `20000104 -> 20260224`

映射到执行库后（本轮范围 2023-01-01 到 2026-03-04）：

- `l1_trade_calendar = 1151`
- `l1_stock_info = 5484`
- `l1_index_daily = 764`
- `l1_stock_daily = 4,014,211`
- `l1_stock_daily` 日期覆盖：`2023-01-03 -> 2026-02-24`

## 3. 执行命令

1. `python main.py build --layers=l2,l3 --start=2023-01-01 --end=2026-03-04`
2. `MIN_AMOUNT=50000 python main.py backtest --start=2023-01-01 --end=2026-02-24 --patterns=bof`

说明：

- `amount` 口径来自 TuShare（千元），本轮使用 `MIN_AMOUNT=50000` 作为运行时参数，避免候选池被误清空。
- `--end` 取 `2026-02-24`，与本地 `raw_daily` 最大日期对齐。

## 4. 运行结果（核心证据）

- `trade_days = 757`
- `signals (l3_signals) = 323`（全部 `bof`）
- `orders = 476`
- `trades = 476`
- `trade_count(配对后) = 238`
- `expected_value = 0.546695`
- `profit_factor = 4.600031718278265`
- `max_drawdown = 0.033860`

补充：

- `l4_orders` 状态分布：仅 `FILLED=476`
- 本轮无 `REJECTED`

## 5. 质量与测试证据

- `pytest -q`：`9 passed`
- 全区间幂等性双跑：`docs/spec/v0.01/v0.01-idempotency-check-20260305.json`
  - `order_id_set_equal=true`
  - `trade_id_set_equal=true`
  - `metrics_equal=true`
  - 两次一致：`EV=0.546695`、`PF=4.600031718278265`、`MDD=0.033860`
- `G6` 证据页：`docs/spec/v0.01/v0.01-g6-asof-evidence-20260305.md`
- 当前测试组织已改为：
  - `tests/unit/<module>/`
  - `tests/integration/<module>/`
  - `tests/patches/<module>/`

## 6. G1-G7 打勾（第2版）

| Gate | 结果 | 说明 |
|---|---|---|
| G1 边界规则写死 | ✅ 通过 | matcher 边界分支与单测在位；本轮真实回放未触发拒单分支。 |
| G2 数据契约写死 | ✅ 通过 | contracts + 主键幂等 + Store as-of 接口在位。 |
| G3 撮合与成本模型固定 | ✅ 通过 | `T+1 Open + 滑点 + 佣金 + 印花税 + 过户费` 已执行。 |
| G4 可复现（重跑一致） | ✅ 通过 | 全区间双跑一致，`l4_orders/l4_trades` 主键集合与关键指标一致。 |
| G5 诚信指标齐全 | ✅ 通过 | 报告已落地 `reject_rate/missing_rate/exposure_rate/failure_reason_breakdown`。 |
| G6 防未来函数/幸存者偏差 | ⚠️ 部分通过 | as-of 约束已在代码；`l1_stock_info` 已改日期快照映射，但源 `raw_stock_basic` 快照频率有限（`effective_from` 到 `2021-12-01`），需补证据说明口径边界。 |
| G7 最小单测通过 | ✅ 通过 | `pytest -q` 通过（9 项）。 |

## 7. 结论

- 当前结论：`NO-GO`（仍有 `G6` 待补证据）
- 已从“无样本不可跑”推进到“全区间可跑且重跑一致”，当前唯一剩余阻塞为 `G6` 证据闭环。

## 8. 下一步（只修 Gate，不加新功能）

1. 固化 `G6` 证据页：说明 `raw_stock_basic` 快照频率边界与对回测口径的影响范围。
2. 增加 `l1_stock_info` as-of 命中率与样本覆盖统计，形成可追溯表格证据。
3. 在不加新功能前提下，仅修 `G6` 所需数据映射与证据链。
