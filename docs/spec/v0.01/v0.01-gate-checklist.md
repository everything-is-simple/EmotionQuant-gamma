# v0.01 开工 Gate Checklist（冻结前置）

**版本**: v0.01 Gate  
**状态**: Active  
**日期**: 2026-03-04  
**关联文档**:  
- `docs/spec/v0.01/v0.01-boundary-deterministic-rules.md`  
- `docs/spec/v0.01/v0.01-data-contract-table.md`  
- `docs/spec/v0.01/v0.01-implementation-card-template.md`

## 1. 开工前 Gate（7 项）

- [ ] `G1` 边界规则写死：停牌/涨跌停/一字板/退市/订单过期均有确定分支，并落到状态机
- [ ] `G2` 数据契约写死：主键/幂等键/as-of/缺失策略/复权口径都有表级定义
- [ ] `G3` 撮合与成本最小模型固定：`T+1 Open + 滑点 + 佣金 + 印花税(卖出) + 过户费`
- [ ] `G4` 可复现：同输入重跑结果一致，且 `_meta_runs` 记录 `config_hash/data_snapshot/git_commit`
- [ ] `G5` 诚信指标齐全：`reject_rate`、`missing_rate`、`exposure_rate`、`failure_reason_breakdown` 可追溯
- [ ] `G6` 防未来函数与幸存者偏差：统一 `trade_date<=asof_date`，股票池按“日期状态”过滤
- [ ] `G7` 最小单测通过：`calendar`、`matcher 成交边界`、`idempotency 重跑`

## 2. 冻结声明（v0.01）

### 2.1 绝不修改（Frozen）

1. 时间语义：`signal_date=T`，`execute_date=T+1`，成交价 `T+1 Open`。  
2. v0.01 触发器：仅 BOF 单形态闭环。  
3. 模块边界契约：`MarketScore/IndustryScore/StockCandidate/Signal/Order/Trade`。  
4. Broker 单内核：回测与纸上交易共用。

### 2.2 允许演化（不破坏 Frozen）

1. 边界规则细化（reject_reason 分类更细）。  
2. `_meta_runs` 追溯字段扩展。  
3. schema migration 脚本完善。  
4. 报告解释性字段增强（不改主契约字段）。

## 3. Go/No-Go 判定

- 若 G1-G7 全部通过：`GO`，进入代码开发。  
- 任一未通过：`NO-GO`，仅允许修 Gate 不允许加新功能。

## 4. 本轮 Gate 复核（2026-03-05）

- [x] `G1` 边界规则写死：通过  
- [x] `G2` 数据契约写死：通过  
- [x] `G3` 撮合与成本最小模型固定：通过  
- [x] `G4` 可复现：通过（`docs/spec/v0.01/v0.01-idempotency-check-20260305-v2.json`）  
- [x] `G5` 诚信指标齐全：通过（`l4_daily_report` 已含诚信四项 + 机会参与指标）  
- [ ] `G6` 防未来函数与幸存者偏差：部分通过（证据见 `docs/spec/v0.01/v0.01-g6-asof-evidence-20260305.md`）  
- [x] `G7` 最小单测通过：通过（`pytest -q`）

当前判定：`NO-GO`（仅剩 `G6`）。
