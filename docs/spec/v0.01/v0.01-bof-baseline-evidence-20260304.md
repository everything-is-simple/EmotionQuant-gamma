# v0.01 BOF Baseline 证据包（第1版）

**日期**: 2026-03-04  
**执行人**: Codex  
**环境**: Windows / Python 3.10  
**数据库**: `G:\EmotionQuant_data\emotionquant.duckdb`

## 1. 执行命令（按约定三步）

1. `python main.py fetch --start=2023-01-01 --end=2026-03-04`
2. `python main.py build --layers=l2,l3 --start=2023-01-01 --end=2026-03-04`
3. `python main.py backtest --start=2023-01-01 --end=2026-03-04 --patterns=bof`

## 2. 运行结果摘要

### 2.1 fetch

- TuShare 调用失败，返回：`您的token不对，请确认。`
- 自动降级到 AKShare 备源（当前为最小骨架实现）
- 写入行数：`0`

### 2.2 build

- L2/L3 构建完成
- upsert 行数：`0`

### 2.3 backtest

- 运行失败
- 错误：`No trade days available in l1_trade_calendar for given range.`

## 3. 数据快照（命令后）

- `l1_trade_calendar=0`
- `l1_stock_daily=0`
- `l1_index_daily=0`
- `l1_stock_info=0`
- `l2_stock_adj_daily=0`
- `l2_market_snapshot=0`
- `l2_industry_daily=0`
- `l3_mss_daily=0`
- `l3_irs_daily=0`
- `l3_signals=0`
- `l4_orders=0`
- `l4_trades=0`
- `l4_daily_report=0`

## 4. 指标证据（EV/PF/MDD/trade_count）

由于无交易日与无交易样本，本轮指标无法生成：

- `EV`: N/A
- `PF`: N/A
- `MDD`: N/A
- `trade_count`: 0

## 5. 参数快照（与本轮相关）

- `start=2023-01-01`
- `end=2026-03-04`
- `patterns=bof`
- `ENABLE_MSS_GATE=False`（默认）
- `ENABLE_IRS_FILTER=False`（默认）
- `ENABLE_GENE_FILTER=False`（默认）
- `tushare_token_set=True`，但 token 校验失败

## 6. G1-G7 打勾表（第1版）

| Gate | 结果 | 说明 |
|---|---|---|
| G1 边界规则写死 | ✅ 通过（代码+测试） | matcher 停牌/涨跌停拒单分支与测试已存在并通过。 |
| G2 数据契约写死 | ✅ 通过（结构层） | contracts + Store 主键/幂等键/as-of 接口已落地。 |
| G3 撮合与成本模型固定 | ✅ 通过（最小模型） | T+1 Open + 滑点 + 佣金 + 印花税(卖) + 过户费已实现。 |
| G4 可复现（重跑一致） | ⚠️ 部分通过 | 合成样本重跑幂等测试通过；真实数据样本尚未验证。 |
| G5 诚信指标齐全 | ❌ 未通过 | 当前最小报告仅有 EV/PF/MDD/trade_count；缺 `reject_rate/missing_rate/exposure_rate/failure_reason_breakdown`。 |
| G6 防未来函数/幸存者偏差 | ⚠️ 部分通过 | 代码已有 as-of 约束接口与日期过滤；但真实数据回放未跑通，无法完成端到端证明。 |
| G7 最小单测通过 | ✅ 通过 | `pytest -q` 结果：8 passed（含 calendar / matcher 边界 / idempotency）。 |

## 7. 结论

- 当前结论：`NO-GO`（实验门未通过）
- 阻塞根因：数据源 token 无效，导致 L1/L2/L3 空库，backtest 无法产生有效样本。

## 8. 下一步（仅修 Gate）

1. 修复 TuShare token（或补全 AKShare 非骨架实现）后重跑三条命令。
2. 重跑后补齐真实样本的 EV/PF/MDD/trade_count。
3. 在报告中补全 G5 指标并复核 G4/G6。
