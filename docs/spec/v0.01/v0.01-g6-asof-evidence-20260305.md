# v0.01 G6 证据页（as-of 命中率与源快照边界）

**日期**: 2026-03-05  
**范围**: `2023-01-01 ~ 2026-02-24`  
**目标 Gate**: `G6 防未来函数与幸存者偏差`

## 1. 复算入口

- 脚本：`scripts/report/build_g6_asof_evidence.py`
- 证据 JSON：`docs/spec/v0.01/v0.01-g6-asof-evidence-20260305-v2.json`
- 执行命令：

```bash
python scripts/report/build_g6_asof_evidence.py \
  --start 2023-01-01 \
  --end 2026-02-24 \
  --output docs/spec/v0.01/v0.01-g6-asof-evidence-20260305-v2.json
```

## 2. as-of 命中率与覆盖统计

### 2.1 `l1_stock_daily` 行级 as-of 命中

- `total_rows = 4,014,211`
- `hit_rows = 4,013,705`
- `miss_rows = 506`
- `hit_rate = 99.9874%`

### 2.2 `l3_signals` 行级 as-of 命中（`code -> ts_code` 映射）

- `total_rows = 323`
- `hit_rows = 323`
- `miss_rows = 0`
- `hit_rate = 100.0000%`

### 2.3 覆盖缺口构成（`l1_stock_daily` 口径）

- `daily_codes = 5,619`
- `info_codes = 5,804`
- `missing_codes = 2`
- `missing_codes_status_breakdown`：
  - `NULL = 2`

解释：退市代码已纳入 `l1_stock_info` 快照；剩余缺口仅为源侧无 `raw_stock_basic` 记录的极少数代码。

### 2.4 信号级状态一致性（新增）

- `signal_count = 323`
- `live_signal_count = 323`
- `non_live_signal_count = 0`
- `non_live_signal_ratio = 0.0`

阈值检查：

- `signal_asof_hit_rate >= 0.999`：通过
- `non_live_signal_ratio <= 0.0`：通过

## 3. 源快照频率边界（影响幸存者偏差证据强度）

来源库 `raw_stock_basic`（`G:\EmotionQuant_data\duckdb\emotionquant.duckdb`）：

- `min_date = 2010-01-01`
- `max_date = 2026-02-01`
- `snapshot_days = 68`
- 相邻快照间隔：
  - `avg_gap_days = 87.69`
  - `p50_gap_days = 31`
  - `p95_gap_days = 334.7`
  - `max_gap_days = 1036`

当前执行库 `l1_stock_info`（change-point 写入后）：

- `min_effective_from = 2010-01-01`
- `max_effective_from = 2021-12-01`
- `effective_days = 7`
- `list_status_distribution`：
  - `L = 5520`
  - `D = 320`

解释：`l1_stock_info` 已是“按日期快照”而非“单点快照”，但受源快照稀疏影响，`effective_from` 新鲜度有限。

## 4. G6 判定（本轮）

- `trade_date <= asof_date` 约束：**通过**
- as-of 命中率（信号级）：**通过（100%）**
- 幸存者偏差证据充分性：**通过**

原因：`list_status` 快照已纳入（含 `D`），并在 selector 侧强制 `list_status='L'` 过滤；信号级非在市比例为 0。

## 5. 结论

本轮 `G6` 结论为 `✅ 通过`。  
证据链已建立并可复算；源快照频率边界仍需在文档中保留说明，但不再阻塞 Gate。
