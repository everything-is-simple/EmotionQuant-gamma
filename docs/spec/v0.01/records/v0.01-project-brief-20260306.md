# v0.01 项目简报（2026-03-06）

**版本**: `历史阶段简报`  
**状态**: `Frozen`  
**封版日期**: `2026-03-06`  
**变更规则**: `仅允许勘误与历史说明补充；不作为当前阶段判断或当前执行入口。`

---

## 1. 文档定位

本文档是 `v0.01` 阶段的一份**历史简报**。

它保留的价值主要是：

1. 记录 `2026-03-06` 当时对项目定位与阶段状态的快照判断。
2. 保留基线跑通后的第一轮管理性总结。
3. 为回看 `v0.01` 从“设计冻结”走向“基线跑通”的时点提供摘要。

本文档不是当前阶段判断依据，也不是当前主线执行入口。

---

## 2. 当前使用边界

当前阅读本文的正确用途是：

1. 回看 `2026-03-06` 当时对 `v0.01` 的阶段认知。
2. 理解历史上“设计已冻结、基线已跑通、周计划仍待推进”这类管理判断。
3. 给 `v0.01` 历史版本补一个简短摘要视角。

当前不应把本文当作：

1. 当前治理状态来源。
2. 当前主线开发判断。
3. 当前项目定位或当前执行计划的最新版本。

---

## 3. 当前权威入口

若要看当前治理状态，请直接查看：

1. `docs/spec/common/records/development-status.md`

若要看 `v0.01 Frozen` 历史执行口径，请查看：

1. `docs/design-v2/01-system/system-baseline.md`

若要看仓库现行设计，请查看：

1. `blueprint/README.md`

---

## 4. 历史保留价值

本文最值得保留的历史信息有三类：

### 4.1 它记录了当时的项目自我定义

原简报保留的关键历史判断是：

1. 项目定位为 A 股低频结构交易系统。
2. 模块分工围绕 `Selector / Strategy / Broker / Backtest/Report` 展开。
3. 当时已经明确 `BOF`、`T+1 Open` 与模块分层的主干语义。

### 4.2 它记录了“基线刚跑通”的阶段感

本文保留的历史快照不是“项目已完成”，而是：

1. 设计已冻结
2. `BOF baseline` 已跑通
3. 但后续周计划仍在推进中

这类判断今天只剩追溯价值，但仍值得保留。

### 4.3 它保留了当时对 `GO baseline` 的第一轮摘要

原文里引用的指标和结论，作为当时阶段性管理摘要仍有历史价值，但它们不能替代后续更完整的 evidence 和 records。

---

## 5. 当前已经失效或容易误导的旧口径

本次整理后，以下内容统一降级：

1. 把本文当成当前项目状态说明。
2. 把本文中的阶段判断当成今天仍然有效的阶段结论。
3. 把简报里引用的指标当成当前代码路径的最新 baseline。

这些判断只代表 `2026-03-06` 当天的管理快照。

---

## 6. 原简报结论保留

原文的历史结论可压缩保留为：

1. 当时项目已不再是纯设计阶段。
2. `v0.01` 基线已经被代码和证据证明“最小闭环可执行”。
3. 但这并不等于 `v0.01` 已完整交付，后续周任务仍待推进。

---

## 7. 相关文档

1. `docs/spec/v0.01/README.md`
2. `docs/spec/v0.01/evidence/v0.01-bof-baseline-evidence-20260305.md`
3. `docs/spec/common/records/development-status.md`
4. `docs/design-v2/01-system/system-baseline.md`
