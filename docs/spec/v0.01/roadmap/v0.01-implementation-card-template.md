# v0.01 实现卡模板（可并行粒度）

**用途**: 把设计任务切成 1-3 天可完成、可验收、可回滚的实现卡。  
**基线**: `docs/design-v2/system-baseline.md`

## 1. 卡片头

- 卡片编号：`CARD-v0.01-xxx`
- 模块：`Data / Selector / Strategy / Broker / Backtest / Report`
- Owner：
- 预计工期：`1-3 天`
- 依赖卡：
- 风险等级：`S0/S1/S2`

## 2. 输入/输出契约

- 输入：
  - 上游表/对象：
  - as-of 约束：
  - 前置状态：
- 输出：
  - 下游表/对象：
  - 主键/幂等键：
  - 失败状态写入：

## 3. 实现内容（最小闭环）

1. 需要新增/修改的文件：  
2. 关键算法/规则：  
3. 边界条件：  
4. 失败分支：  
5. 必要注释点（关键业务逻辑、状态机、时序约束）：

## 4. 验收标准（DoD）

1. 功能验收：  
2. 数据验收：  
3. 幂等验收：  
4. 时序验收（T+1 / as-of）：  
5. 报告验收（可解释性）：

## 5. 测试与证据

- 最小三测（强制）：
  - `calendar`：交易日推进（`next_trade_date`）正确
  - `matcher 边界`：停牌/涨跌停/一字板撮合分支正确
  - `idempotency`：同输入重跑主键集合不变
- 单测用例：  
- 集成用例：  
- 回归命令：  
- 关键输出快照（指标/行数/状态分布）：

## 6. 回滚与兼容

1. 回滚策略：  
2. 与 Frozen 口径一致性说明：  
3. 后续版本兼容点（v0.02+）：

## 7. 完成记录

- 完成时间：
- 代码变更摘要：
- 评审结论：`GO / NO-GO`




