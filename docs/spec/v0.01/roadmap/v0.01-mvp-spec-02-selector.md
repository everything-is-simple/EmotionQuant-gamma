# Spec 02: Selector

> **版本**: v0.01 | **状态**: Active | **REV-ID**: `REV-v0.01-S02-20260304` | **基线**: `docs/design-v2/system-baseline.md` | **评审标准**: `docs/design-v2/sandbox-review-standard.md`

## 需求摘要
从全市场 ~5000 股中筛选 50-100 只候选股。

v0.01 固定采用两阶段扫描（SoT：`docs/design-v2/selector-design.md`）：
- **Stage 1 粗筛**（~5000 → ~200）：停牌/上市天数/流动性/基础波动 等基础过滤（目标是降低计算规模与 API 压力）
- **Stage 2 精筛**（~200 → 50-100）：在粗筛池内执行 MSS 门控 + IRS 行业过滤（v0.01 的 `gene` 仅保留接口，不进实时漏斗）→ 输出候选池给 Strategy

说明：PAS 形态触发器扫描由 `strategy.py` 在候选池上执行，不在全市场扫描，也不在 selector 内实现。

**设计文档**: `docs/design-v2/selector-design.md`, `docs/design-v2/architecture-master.md` §4.2

## 交付文件

| 文件 | 职责 |
|------|------|
| `src/selector/__init__.py` | 包初始化 |
| `src/selector/normalize.py` | zscore_normalize 共用归一化函数 |
| `src/selector/baseline.py` | MSS/IRS baseline 硬编码（第1迭代） |
| `src/selector/mss.py` | MSS 六因子市场温度评分 |
| `src/selector/irs.py` | IRS 二因子行业轮动评分 |
| `src/selector/gene.py` | 牛股基因画像（第2迭代，先建骨架） |
| `src/selector/selector.py` | 漏斗编排 → 输出候选池 |

## 设计要点

### zscore_normalize（三系统共用）
```
zscore_normalize(value, mean, std) → 0-100
  std == 0 → 50（中性分）
  z = (value - mean) / std
  return clip((z + 3) / 6 × 100, 0, 100)
```
第1迭代 mean/std 硬编码在 baseline.py，后续改为 120 日滚动窗口。

### MSS 六因子
- 基础三因子（85%）：大盘系数(17%) + 赚钱效应(34%) + 亏钱效应(34%)
- 增强三因子（15%）：连续性(5%) + 极端因子(5%) + 波动因子(5%)
- 亏钱效应和波动因子是负面指标，温度公式中取 `100 - x` 翻转
- 输出：score(0-100), signal(≥65 BULLISH / ≤35 BEARISH / else NEUTRAL)
- 所有 ratio 必须用 safe_ratio（分母为零保护）

### IRS 二因子
- 相对强度(55%)：industry_pct_chg - benchmark_pct_chg(000001.SH)
- 资金流向(45%)：10日滚动净流入 + 资金占比
- 输出：每日 31 个行业评分 + 排名
- 后续迭代预留 4 个函数签名（返回 0）

### selector.py 漏斗
1. Stage 1：粗筛（_apply_basic_filters）→ 停牌/次新/流动性/基础波动 等过滤，约 5000→200
   - 强制补充：股票池必须按 `asof_date` 读取，并显式过滤 `list_status='L'`
   - 流动性字段涉及 `amount` 时，统一按 TuShare `千元` 口径解释
2. Stage 2：MSS 开关（ENABLE_MSS_GATE）→ BEARISH 当日不出手（可在实现中前置以便早退，但口径归入 Stage 2）
3. Stage 2：IRS 过滤（ENABLE_IRS_FILTER）→ 在粗筛池内仅保留 Top-N 强势行业
4. 基因过滤（ENABLE_GENE_FILTER=False）→ 第2迭代（v0.01 默认禁用）
5. 候选排序与截断：构造 `list[StockCandidate]`，按 score 排序，取 Top-N 输出给 Strategy
- 候选池为内存 `list[StockCandidate]`，不落库
- 候选解释性不能丢失：至少需能追溯 `filters_passed`、`reject_reason`、`liquidity_tag`

## 实现任务

### 共用工具
- [x] 实现 `normalize.py`（zscore_normalize，向量化版本 + 单值版本）
- [x] 实现 `baseline.py`（MSS_BASELINE、IRS_BASELINE 硬编码占位值）
- [x] 实现 safe_ratio（单值 + 向量化 np.where 版本）

### mss.py
- [x] 实现 `compute_mss(store, start, end)`（批量读 l2_market_snapshot，向量化计算）
- [x] 实现 `compute_mss_single(row, baseline)` → MarketScore（纯函数，便于单测）
- [x] 六因子管线：各 ratio 计算 → zscore → 温度合成 → 信号判定
- [x] 批量写入 l3_mss_daily
- [x] 单测：构造 mock l2_market_snapshot 行，验证分数和信号

### irs.py
- [x] 实现 `compute_irs(store, start, end)`（读 l2_industry_daily + l1_index_daily）
- [x] 实现 `compute_irs_single(industry_df, benchmark_df, date, baseline)` → IndustryScore
- [x] 相对强度：industry_pct_chg - benchmark_pct_chg → zscore
- [x] 资金流向：amount_delta 10日滚动 + flow_share → 加权
- [x] 行业排名：groupby(date).rank(ascending=False)
- [x] 批量写入 l3_irs_daily
- [ ] 预留 4 个扩展因子函数签名（返回 0）
- [x] 单测：构造 mock 行业数据+基准，验证排名正确性

### gene.py（骨架）
- [x] 实现 `compute_gene(store, start, end)` 函数签名（内部 pass，第2迭代填充）
- [x] 在 config.py 中设置 `ENABLE_GENE_FILTER = False`

### selector.py（两阶段扫描：粗筛 5000→~200 + 精筛 MSS/IRS（~200→50-100））
- [x] 实现 `select_candidates(store, calc_date)` → list[StockCandidate]
- [x] Stage 1: 粗筛（_apply_basic_filters：停牌/上市天数/ST/次新/流动性/基础波动/市值）得到 ~200
  - 强制条件：查询必须带 `asof_date`，并显式过滤 `list_status='L'`
- [x] Stage 2: MSS 开关检查（读 l3_mss_daily，BEARISH 当日不出手）
- [x] Stage 2: IRS 过滤（读 l3_irs_daily，取 Top-N 行业，并与粗筛池取交集）
- [ ] 基因过滤（预留，ENABLE_GENE_FILTER=False 时跳过）
- [x] 构造候选 `score`（流动性40%+结构稳定30%+行业优先30%，仅用于候选排序，不参与PAS触发）
- [x] 按 score 降序排序，取 Top-N（50-100）输出给 Strategy
- [x] 各步日志输出过滤数量
- [x] 候选解释性落地（追踪表或报告字段）：`filters_passed` / `reject_reason` / `liquidity_tag`
- [x] 单测：mock Store 返回预设数据，验证每级过滤

### 集成验证
- [x] 用真实 L2 数据运行 compute_mss + compute_irs，验证分数分布合理
- [x] 运行 select_candidates，验证候选池 50-100 只
- [x] 消融对照A：关闭 MSS/IRS，仅 BOF baseline（输出指标）
- [x] 消融对照B：开启 MSS，关闭 IRS（输出指标）
- [x] 消融对照C：开启 MSS+IRS（输出指标）
- [ ] 三组结果按同口径对比（胜率/盈亏比/期望值/最大回撤/分环境中位数）

## 验收标准
1. `compute_mss_single` 输入全零 → score ≈ 50（中性）
2. `compute_irs` 输出 31 个行业排名无重复
3. MSS=BEARISH 时 selector 返回空列表
4. limit_up_count=0 时 broken_rate=0（不是 NaN）
5. 全市场无交易数据时返回空候选池，不报错
6. 漏斗消融实验三组结果可复现且可比较
7. 候选池只包含 `asof_date` 当日处于 `list_status='L'` 的股票
8. 流动性阈值按 `amount` 千元口径配置时，结果可复现且不出现单位错配

## 校对证据（2026-03-04）

### 校对范围

1. `docs/design-v2/selector-design.md`
2. `docs/design-v2/system-baseline.md`
3. `docs/spec/v0.01/roadmap/v0.01-mvp-roadmap.md`
4. `.kiro/steering/architecture.md`
5. `.kiro/steering/product.md`
6. `.kiro/steering/conventions.md`
7. `AGENTS.md` / `CLAUDE.md` / `WARP.md`

### 校对结论

1. 两阶段扫描口径一致：`~5000 -> ~200 -> 50~100`，且形态扫描边界在 Strategy，不在 Selector。
2. MSS/IRS/gene 开关语义与 SoT 一致：`gene` 在 v0.01 仅保留接口，不入实时漏斗。
3. 消融顺序一致：`BOF baseline -> BOF+MSS -> BOF+MSS+IRS`，无顺序漂移。

### 本轮修订记录

1. 新增本节“校对证据”，沉淀可追溯校对范围与结论。
2. 未修改任何执行语义、阈值和模块边界。
3. 基于基线跑通经验，补充在市过滤、`asof_date`、`amount` 单位与候选解释性这四项实现约束。
4. 2026-03-06 补回实现进度勾选：MSS/IRS 主链、候选解释性、三组短窗消融入口与证据文件已落地。
5. 2026-03-06 补充真实分布审计：MSS 已从 `all NEUTRAL` 修正为 `BULLISH=20 / BEARISH=23 / NEUTRAL=714`，IRS 已从 `111` 桶收口到正式 `31` 桶，并已修正日内唯一排名。
6. 2026-03-06 补充 MSS 阈值短窗消融：在当前 `BEARISH-only` gate 语义下，bullish threshold `55/58/60/62/65` 对 trade metrics 为 no-op。

## 已知风险与偏差

| 级别 | 问题 | Owner | 截止日期 | 状态 |
|------|------|-------|----------|------|
| S2 | 在当前 `BEARISH-only` gate 语义下，bullish threshold `55/58/60/62/65` 只改变环境标签，不改变交易阻断；继续做 bullish threshold sweep 对 trade metrics 信息量有限 | Codex | Week 2 结束前 | open |
| S2 | IRS 已回到正式 `31` 行业，但 `Top N` 强度（10/15/20）尚未重新消融校准 | Codex | Week 2 结束前 | open |
| S3 | 全周期三场消融在 `--skip-rebuild-l3` 条件下运行超过 `75` 分钟仍未收口；当前仅有短窗对照证据 | Codex | Week 2 结束前 | open |




